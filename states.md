# Estado Atual do Sistema

## Stack Tecnológica

- Java 17 com Spring Boot 4.1.0 e Maven Wrapper.
- Spring Cloud OpenFeign para integrações REST externas.
- Spring Web MVC para endpoints auxiliares de auditoria e quarentena.
- Spring Web Services, Jakarta XML Web Services API, JAX-WS RI e `jaxws-maven-plugin` para clientes SOAP da Vedacit/MultiTMS gerados a partir de WSDL.
- Spring Data JPA/Hibernate com Microsoft JDBC Driver para SQL Server.
- Banco SQL Server exclusivo `SATELITE_TMS_AUDITORIA`/`satelite_tms_auditoria` para auditoria, cursores e estado técnico da integração.
- Apache PDFBox e Java ImageIO/AWT para renderização, conversão, compressão e normalização de canhotos.
- WSDL/XSD da Vedacit empacotados em `src/main/resources/wsdl/vedacit` para criação de proxies SOAP dentro do Fat JAR.
- Lombok em entidades/modelos internos; testes com starters de teste Spring Boot para JPA, Web MVC e Web Services.
- Operação em Windows por `satelite.bat`, `build.bat` e scripts em `scripts/`; banco provisionado por `database/subir_database.bat`.

## Arquitetura e Padrões

- Aceite real de comprovantes confirmado em 10/09/2026 às 23:30: a etapa XML retornou às 23:27:52 após 18 páginas/360 ocorrências e 304 CT-es com sucesso. Houve oito resultados de erro na última página, incluindo seis downloads XML ESL com HTTP 401; o cursor foi preservado, sem esgotamento da origem. A sincronização seguinte liberou os novos comprovantes: pendentes 1→280, bloqueios 836→557. Três aceites SOAP de canhoto ocorreram às 23:28:26, 23:29:06 e 23:29:38; o endpoint passou de 267 para 270 comprovantes no período, com 277 pendentes às 23:29:55. Os tempos das chamadas SOAP foram aproximadamente 2,5/36,6/28,2 segundos, além do preparo e pausa; o dreno seguia em execução. A espera inicial por toda a varredura XML atrasou os comprovantes cerca de 15 minutos. Confirmação de operação não significa fila/planilhas esgotadas. Nenhuma mudança de código de aplicação, credencial, banco ou runtime foi feita na inspeção. Relatório e evidências em `docs/verificacao-xml-apos-reinicio-2026-09-10.md` e `target/verificacao-comprovantes-20260910-2319/`.
- Auditoria dos comprovantes em 10/09/2026 às 23:26: o worker executa toda a varredura XML de forma síncrona antes de sincronizar/liberar a fila SFTP. As dez páginas são apenas intervalo de pacing de 30 segundos; a página 11 começou após a pausa e a execução chegou à 16 sem entrar nos comprovantes. SQL somente leitura confirmou 275 CT-es com XML novo e zero comprovantes desde o start; 252 pares SFTP com XML datado no ciclo ainda permaneciam `BLOQUEADO_ORIGEM`/`PENDENTE_FOTO`, pois a promoção implementada só roda na etapa posterior de inventário. Isso demonstra espera por ordenação das etapas, sem provar nova recusa de comprovantes. Três sucessos ativos exatos continuam aguardando conciliação; 19 resultados ambíguos e uma recusa permanecem retidos. Worker PID 28988 online, nenhum WARN/ERROR observado, total de comprovantes no período ainda 267. Não houve alteração de aplicação/runtime nem escrita no banco nesta auditoria. Relatório `docs/verificacao-xml-apos-reinicio-2026-09-10.md`; evidências `target/verificacao-comprovantes-20260910-2319/`.
- Primeiro envio XML confirmado em produção após ativação: start humano em 10/09/2026 às 23:13:06, worker ID 189/PID 28988, com as seis flags aplicadas. O fluxo iniciou evento 110/cursor `VEDACIT_XML` às 23:13:27, leu a origem e começou a receber aceites SOAP às 23:15:14. Às 23:17:25 foram conferidos 37 XMLs de CT-es distintos com sucesso no log e os mesmos 37 no endpoint de indicadores (antes zero); nenhuma linha ERROR no ciclo observado. Duas páginas concluídas, cursor avançando e processo ainda online; comprovantes permaneciam 267 no período porque a etapa XML ainda não terminara. A amostra comprova envio efetivo acima de dez na mesma execução, sem afirmar conclusão da fila. Leitura de threads mostrou obtenção XML no SFTP, sem erro; não foi feita chamada externa adicional nem controle do worker pela IA. Evidências em `target/primeiro-ciclo-xml-20260910-2314/`. O resumo genérico de página subconta XML quando o canhoto permanece `PENDENTE_FOTO`; o endpoint por etapa contou corretamente, e a telemetria de resumo precisa de ajuste separado.
- Configuração XML aplicada ao cadastro efetivo PM2 em 10/09/2026 às 21:36 BRT, por autorização explícita do usuário, deixando o início para ele: `WORK_SFTP_CLIENTES_XML_ENABLED=true`, `SFTP_RODOGARCIA_ENABLED=true`, `VEDACIT_SEND_CTE_XML_ENABLED=true`, `WORK_SFTP_CLIENTES_DRAIN_ENABLED=true`, interrupção após três erros consecutivos e intervalo XML de 1.000 ms. O worker foi recadastrado sem executar Java pelo ramo `prepare/status=stopped` do PM2 7.0.1, conferido previamente em simulação com execução de processo proibida. Nome `WORK-SFTP-CLIENTES` preservado; ID mudou de 28 para 189, com status `stopped`, PID 0 e início manual habilitado. Configuração persistida exclusivamente na entrada desse worker em `dump.pm2`; demais processos/configurações/logs preservados. API Satélite segue PID 37668, HTTP 200/versão 1; JAR segue `6C5976AE...`. Backups privados ignorados pelo Git e evidências em `target/ativacao-worker-xml-20260910/`. Não houve início do worker, envio ou escrita SQL; o primeiro ciclo/aceite SOAP ainda depende do start humano.
- Verificação operacional em 10/09/2026 às 21:27–21:30 BRT: o pacote `6C5976AE...` está em uso após reinício humano às 21:22 e `indicadores-etapas` retorna HTTP 200/versão 1. A captura do usuário coincide com a API: setembro XML 0/POD 267, pendentes 4, bloqueados 1.614 e sem confirmação/classificação 1.520. No recorte 11/08–10/09, Vedacit tem 75 XMLs/2.133 comprovantes confirmados; a série XML termina em 20/08. O worker continua sem `WORK_SFTP_CLIENTES_XML_ENABLED` no cadastro PM2 (padrão false), e o `.env` mantém `SFTP_RODOGARCIA_ENABLED=false`, sem sobreposição no processo. O arquivo ecosystem preparado ainda não foi aplicado ao cadastro. Ciclo 21:23:14–21:24:00: SFTP OK, 3.108 arquivos reconhecidos, um selecionado sem comprovante do CT-e exato, zero envios; sem execução XML. Código de saída 0/espera PM2 de 30 minutos. O dreno já é true por padrão; não houve lote de dez atingido. Relatório `docs/verificacao-xml-apos-reinicio-2026-09-10.md`. Apenas leitura/documentação; ativação e recuperação ainda pendentes.
- Pacote operacional atualizado em 10/09/2026 às 21:19 BRT, por solicitação explícita do usuário: `target/satelite-0.0.1-SNAPSHOT.jar` agora possui SHA-256 `6C5976AE48C418D4D8C5E2408E035DA49218B9F604C9D31A3B67234338DCADF6`. Compilação Java 17 e empacotamento concluídos às 21:15, com 42 testes direcionados aprovados e rede bloqueada nos testes; 1.249 arquivos conferidos contra a compilação testada, rota `indicadores-etapas` presente e H2 ausente. A primeira substituição foi recusada pelo Windows porque o JAR estava em uso. Depois que o usuário parou os dois processos (ambos `stopped`, PID 0), a troca atômica e a conferência do hash foram concluídas. Backup `EBC700C7...` e evidências em `target/publicacao-pm2-20260910-2114/`; compilação em `target/unit-tests/coverage-20260910-211250-997/`. O usuário também autorizou parar os dois processos, mas não foi necessário fazê-lo pela IA. Reinício e validação HTTP 200 permanecem com o operador; configurações/ambiente do PM2 não foram alterados nem houve envios nesta publicação.
- Diagnóstico do 404 após reinício humano em 10/09/2026, 20:13 BRT: PM2 ainda carrega o JAR operacional de 09/09, SHA-256 `EBC700C7...`, sem a rota `indicadores-etapas`; GET direto em `127.0.0.1:19090` reproduziu 404. A API Dashboard já usa JAR de 10/09, `196D4E36...`, com a rota nova. O candidato Satélite `target/indicadores-etapas-20260910/satelite-0.0.1-SNAPSHOT.jar` (`45CEF40A...`) existe e contém a rota. Falta publicação humana do Satélite e conferência HTTP 200/versão 1; reiniciar o pacote antigo não aplica código novo. API e worker compartilham o JAR principal; preservar o controle humano de ambos e o primeiro lote supervisionado. Nenhuma alteração operacional, SQL ou envio nesta investigação. Evidência sanitizada em `target/diagnostico-404-etapas-20260910/evidence.json`; procedimento atualizado no documento conjunto do Dashboard.
- Indicadores por etapa concluídos em 10/09/2026: rota `GET /api/auditoria/integracoes-clientes/indicadores-etapas` agrega em SQL por status/data próprios, sem fallback geral e sem excluir bloqueios do saldo. XML Vedacit é deduplicado por CT-e; comprovante por NF-e/CT-e efetivo; demais destinos por ocorrência. Confirmação datada prevalece sobre cópias pendentes, preservando a primeira data disponível. Saldo atual separa pendentes, bloqueados e casos sem confirmação/classificação, sem filtro temporal; ignorados/não aplicáveis e arquivados não viram envios. A tela Dashboard usa exclusivamente esse contrato para gráficos/cards e mostra datas individuais na auditoria. Não reconstitui todas as tentativas ou históricos sobrescritos. Validação: 42 testes direcionados aprovados, incluindo queries em H2 e contexto passivo sem rede; quatro statements SQL Server reais em somente leitura/rollback, com paridade resumo/série e SQL XML independente. Recorte 01–10/09: 0 XMLs e 267 comprovantes com confirmação datada. Novo candidato `target/indicadores-etapas-20260910/satelite-0.0.1-SNAPSHOT.jar`, SHA-256 `45CEF40A497725004A4F141C7E42DF34AD83C809A41A438B817EDF12579ED550`; 1.249 arquivos de aplicação iguais aos testados, sem H2 no pacote. Dashboard aprovou 703 testes backend, 355 frontend e oito cenários visuais. Documentação conjunta em `../etl-dash/dashboards/docs/correcao-indicadores-etapas-2026-09-10.md`. Sem migration ou alteração operacional; publicação humana pendente.
- Auditoria cruzada do Dashboard em 10/09/2026, 19:21 BRT: GET local para 01–10/09 retornou 273 registros Vedacit, XML 100% e canhoto 98,53%; a execução isolada das funções reais do frontend reproduziu 273 nas duas barras, 546 processados e 542 sucessos. Mesmo com XML simulado em zero, as barras permanecem iguais porque copiam `totalRegistros`. A query do Satélite filtra pela atualização geral, lê status atuais e exclui bloqueios; isso não comprova envios XML no período. Sazonalidade considera sucesso de qualquer etapa; Saúde usa só XML e Pendências deriva da série. O endpoint legado de resumo tem limitações equivalentes. Evidências em `target/auditoria-apresentacao-integracoes-20260910/`; relatório em `../etl-dash/dashboards/docs/auditoria-apresentacao-integracoes-2026-09-10.md`. Apenas auditoria e documentação, sem mudanças matemáticas, SQL, JAR ou runtime.
- Avisos Java corrigidos em 10/09/2026: `RecuperacaoDirigidaVedacitRunner` interrompe explicitamente a página nula antes de acessar dados/paginação; `VedacitSftpClient` coleta nomes com verificação de nulidade, eliminando a conversão da referência de método; os três testes indicados não importam mais `ArgumentMatchers.*` redundantemente. Compilação Java 17 e 38 testes direcionados aprovados, sem falhas/erros e com rede bloqueada. Candidato atualizado em `target/vedacit-correcao-20260910/avisos-java/satelite-0.0.1-SNAPSHOT.jar`, SHA-256 `8976BDACF3BB3C111C7EA5547C7CC79B4A253B8A7D86A269C19317D4743AD1DD`; os 1.245 arquivos de aplicação coincidem com a saída testada `target/unit-tests/coverage-20260910-190928-884/`. A suíte completa anterior permanece como baseline, sem nova execução integral nesta limpeza. JAR operacional e processos preservados; ativação continua pendente do operador.

- Configuração do candidato Vedacit de 10/09: `ecosystem.config.js` local, ignorado pelo Git, foi preparado sem iniciar/reiniciar processos; `ecosystem.config.example.js` contém a cópia idêntica sem credenciais para revisão/versionamento. A instalação precisa aplicar essas flags ao cadastro do worker no PM2, preservando a API passiva. Apenas editar o arquivo não altera os argumentos/ambiente já carregados pelo daemon.

- Correção Vedacit preparada em 10/09/2026: rejeição SFTP deixa de criar sucesso de XML; a etapa XML exige resposta SOAP positiva e usa exclusão por CT-e/consulta histórica, sem retry imediato de resultado ambíguo. Resposta de comprovante sem confirmação fica retida. O worker pode executar o fluxo XML de evento 110/cursor próprio antes dos comprovantes; `ecosystem.config.js` prepara `WORK_SFTP_CLIENTES_XML_ENABLED=true` e SFTP XML habilitado. O dreno percorre lotes de dez até esgotar a passagem, com pausas, consultas de até 500 chaves, uma tentativa por NF-e e interrupção após três erros consecutivos. Confirmações ativas do par exato também reconciliam a fila, preservando datas; o índice XML guarda somente metadados/identificação. A recuperação dirigida percorre todas as páginas e aceita manifestos NF-e;CT-e sem truncamento, em prévia por padrão. Manifestos locais: 695 pares XML para recuperação, 74 divergências retidas e 101 NF-es sem comprovante SFTP. A consulta nova passou em SQL somente leitura com rollback (3.021 NF-es/37 leituras/máximo 502 parâmetros). Documentação: `docs/correcao-fluxo-vedacit-2026-09-10.md`. Validação final: 327 testes, 324 aprovados, zero falhas/erros e três manuais não executados, com rede bloqueada. Pacote isolado target/vedacit-correcao-20260910/satelite-0.0.1-SNAPSHOT.jar, SHA-256 CD02D0FAF287B6EAE5078290FCF032CF5A1E6E7793BD033C5D735CCEC9B5F940; 1.245 arquivos de aplicação idênticos aos testados. A materialização agora consulta NF-e e CT-e, preservando comprovantes independentes quando duas NF-es compartilham CT-e; restrições históricas de canhoto também são preservadas. Consulta SQL final novamente aprovada em somente leitura com rollback. Evidências em target/vedacit-correcao-20260910/. JAR operacional EBC700C7... e controle de runtime preservados; instalação e aceite SOAP reais ainda dependem da etapa humana.

- Conciliação final das duas planilhas em 10/09, 18:03–18:05 BRT: `falta_comprovante_vedacit.xlsx` contém 798 NF-es distintas, todas com `ESL Comprovante=Sim` informado pelo usuário. Destas, 695 possuem um CT-e com comprovante SFTP e estão bloqueadas por XML/dados; 629 também têm XML correlacionado disponível e 66 não. Outras 101 não possuem comprovante SFTP, sendo 71 sem auditoria e 30 com histórico; o worker exclusivo não consulta ESL. Duas têm sucesso local de canhoto e exigem conciliação com a ausência informada pela Vedacit. Nenhuma NF-e apresentou comprovantes SFTP de múltiplos CT-es simultaneamente, mas uma bloqueada tem mais de um CT-e auditado. Há 321 NF-es em ambas as relações, portanto os volumes não são somáveis. A planilha de XML manteve 618 disponíveis sem sucesso, 77 sem XML/sucesso e 74 divergências de sucesso local. Cópias com classificação por linha em `target/auditoria-envios-20260910/falta_XML_vedacit_auditado.xlsx` e `falta_comprovante_vedacit_auditado.xlsx`; relatório em `docs/auditoria-envios-xml-vedacit-2026-09-10.md`. Foram apenas leituras SQL/SFTP, reprodução em memória e documentação; originais, código operacional, JAR, configuração e processos preservados.
- Complemento da auditoria em 10/09, 17:55–17:59 BRT, com `falta_XML_vedacit.xlsx`: 769 pares distintos e com chaves válidas, dos quais 618 têm XML correlacionado no SFTP sem sucesso local, 77 não têm XML reconhecido nem sucesso e 74 divergem da informação de ausência na Vedacit por possuírem `status_dados=SUCESSO`. Dos 74, 52 não têm data de etapa XML em qualquer sucesso nem sucesso arquivado; 22 têm etapa histórica datada. Foi reproduzido em memória, sem rede/banco, o falso sucesso no caminho de rejeição SFTP: comprovante instável recebe dados `SUCESSO`; ao estabilizar, entra em `PENDENTE_ENVIO` sem XML enviado. A falha está em `EtlRepescagemService.sincronizarInventarioSftpVedacit(String, VedacitSftpInventory)` e na promoção que confia nessa flag. Isso não atribui automaticamente os 52 casos à mesma causa. Na planilha, 333 CT-es não têm auditoria; 352 dos 618 XMLs disponíveis também têm comprovante por metadados e estão bloqueados. A segunda leitura manteve 1.115 CT-es SFTP sem sucesso, com 419 sem auditoria após uma nova materialização operacional; os bloqueados passaram para 775, dos quais 699 com XML correlacionado. Os 20 arquivos não reconhecidos não possuem `infCte` único no namespace fiscal, sem falha de parse. Relatório atualizado em `docs/auditoria-envios-xml-vedacit-2026-09-10.md`; nenhuma correção, envio ou alteração operacional foi executada.
- Auditoria de volume/XML Vedacit em 10/09/2026, leituras entre 17:42–17:49 BRT: o worker continua limitado a dez itens por ciclo, com reinício PM2 30 minutos após o término, sem contador diário e sem dreno contínuo. O log consolidado completo disponível (21.738 linhas, 21/08–10/09) e SQL por data de término confirmam 168/87/10/4 envios em 07/08/09/10 de setembro, sendo 10/09 parcial até 17:24; em 09/09 e 10/09 nenhuma seleção chegou a dez. A limitação atual é principalmente a etapa XML: o processo observado envia apenas comprovantes, enquanto 774 NF-es estão em `PENDENTE_ORIGEM`/`BLOQUEADO_ORIGEM`. A leitura dos 1.496 XMLs SFTP identificou 1.476 CT-es distintos no conteúdo: 361 com sucesso de dados histórico e 1.115 sem confirmação local, dos quais 420 sem qualquer auditoria. Vinte arquivos ficaram fora do formato CT-e reconhecido pela sondagem, sem inferir que todos sejam XML malformado. Entre os 774 bloqueados, 698 possuem XML correlacionado e comprovante por metadados; três bloqueados possuem sucesso ativo para o mesmo par e precisam de conciliação sem reenvio. A última etapa XML com sucesso registrada, incluindo arquivados, é de 20/08 às 14:28. O gráfico conta sucesso de qualquer etapa por data de atualização e exclui bloqueios: os seis sucessos de 10/09 incluem quatro canhotos enviados, um timeout e uma pendência. A única pendência normal possui NF-e no inventário, mas não o comprovante do CT-e exato. Não houve ESL/SOAP, escrita SQL/remota, persistência de bytes fiscais, mudança de configuração, JAR ou runtime. Relatório e limites: `docs/auditoria-envios-xml-vedacit-2026-09-10.md`; evidências sanitizadas em `target/auditoria-envios-20260910/`.
- Integrações/origem dos comprovantes em 09–10/09/2026: `WorkSftpClienteStatusDTO` expõe `origemComprovantes=SFTP`, derivada do escopo exclusivo de `tb_work_sftp_cliente_execucao`, sem inferir origem histórica de XML ou canal de envio. O histórico aplica `origem` tanto à página quanto à contagem; `API_ESL` retorna vazio porque os ciclos dos outros processos não estão nesta auditoria. A leitura HTTP em 10/09 confirmou que o JAR operacional `EBC700C7...` ainda responde sem o campo. O adaptador do Dashboard identifica como SFTP o campo ausente apenas nessas rotas exclusivas, preserva valores explícitos e impede exibir página incompatível quando o filtro é ignorado. Seus 21 testes confirmaram inclusive API ESL na coluna e no filtro quando enviada na resposta simulada; isso não equivale a monitorar fallbacks reais de outros processos. O worker exige `VEDACIT_SFTP_RECEIPT_ONLY=true`; a obtenção de canhoto interrompe antes do fallback ESL nesse modo. Não houve migration, escrita SQL, chamada ESL/SFTP/SOAP nem alteração do worker. Os 27 testes Java direcionados anteriores passaram com rede bloqueada; candidato em `target/unit-tests/coverage-20260909-235105-562/satelite-0.0.1-SNAPSHOT.jar`, SHA-256 `FA925E2E524BDECB72AD171FE3741A120580172F07C4EE08790103C65BE5A2B4`. JAR e runtime operacionais preservados.
- Pacote disponibilizado em 09/09/2026 às 23:31 BRT após o operador parar `WORK-SFTP-CLIENTES` e `Satelite-API-19090`: recompilação Java 17 e empacotamento concluídos com `BUILD SUCCESS` em `target/release-build/20260909-232906`, reutilizando sem edição os 971 fontes SOAP recém-gerados na validação anterior. Os 1.458 arquivos internos do novo JAR (classes, recursos, bibliotecas e launcher) são idênticos por SHA-256 aos do candidato validado; os fontes permanecem inalterados, portanto a suíte de 300 aprovados não foi repetida. O JAR padrão `target/satelite-0.0.1-SNAPSHOT.jar` foi substituído atomicamente e conferido, SHA-256 `EBC700C71B264A50EC933FDD290B3761DBB915152420AE5D6626746ADB7F60E2`. Backup anterior em `target/release-build/20260909-232906/satelite-anterior-A77DB9AD.jar`, SHA-256 `A77DB9AD8428E5B47553955D0534EB3F83B7511B5E9404A2EB17BE8EEB937751`. Nenhum processo foi iniciado/reiniciado pela automação; o operador retomará os dois processos. Evidências: `build.log`, `content-comparison.json` e `installation.json` nessa pasta. A confirmação de envio real continua pendente de comprovante elegível.
- Validação do pacote em 09/09/2026: novo JAR Java 17 isolado em `target/release-validation/20260909-225338/satelite-0.0.1-SNAPSHOT.jar`, SHA-256 `A136ADA9C7E2FFDD3197A306CE26BC9E213388D476D3CC657399D6A054EA115D`. A geração automática dos três clientes SOAP a partir dos WSDLs oficiais, compilação e empacotamento passaram. A suíte do candidato teve 303 testes: 300 aprovados, zero falhas/erros e três manuais ignorados, em 51 classes, com rede bloqueada. Os 1.243 arquivos de aplicação empacotados coincidem por conteúdo com a saída testada. O JAR operacional foi preservado; não houve staging, commit, deploy nem alteração de runtime/configuração. Parecer técnico favorável ao commit e pacote; a confirmação de envio real permanece pendente por ausência de comprovante elegível. Evidências em `docs/validacao-pacote-2026-09-09.md`.
- A validação dirigida `scripts/validacao/SqlSftpReadOnlyProbe.java` executou as consultas reais do repositório e o particionamento em `SATELITE_TMS_AUDITORIA`, sem iniciar Spring: 2.979 NF-es auditadas distintas, seis blocos por fila e máximo de 502 parâmetros por statement. Seleção normal (1), técnica (0), ordem/deduplicação e saldo (1) coincidiram com SQL independente na mesma transação serializável, com timeout de lock de 1.500 ms e rollback. O JDBC bloqueou DML/DDL/commit; não houve escrita. A massa real atual tem apenas um candidato, portanto os cenários com vários candidatos ordenados continuam complementados pelos testes unitários.
- Os snapshots WSDL/XSD oficiais de CT-e, NF-e e ocorrências passam a integrar o conjunto para commit: eram ignorados pelo Git, embora necessários aos proxies locais. O snapshot de ocorrências foi atualizado por leitura do contrato oficial (sete arquivos com diferença semântica), incluindo `IntegracaoDocumentoComplementar`; classes geradas não foram editadas. `VedacitSoapContractTest` cria os três proxies reais com recursos locais. A mesma criação passou usando as bibliotecas e recursos dentro do JAR candidato, com rede bloqueada; não equivale a autenticação ou aceite SOAP remoto.
- A prévia real com o novo cliente SFTP em 09/09/2026 encontrou 3.058 arquivos válidos, 37 rejeitados e um candidato SQL, sem metadado/comprovante correspondente disponível. Nenhum item estava pronto para envio. O ciclo operacional já existente registrou a mesma pendência sem consultar ESL. O ciclo supervisionado do novo JAR, limitado a dez itens, permanece condicionado a comprovante elegível e condução humana do runtime; não reenviar sucessos, bloqueios ou timeouts ambíguos para produzir massa de teste.

- Novo painel fornecido em 09/09/2026 confirma 4.641 chamadas `/graphql` do API Desenvolvedor no acumulado exibido, contra 22.342 em 08/09 (79,23% abaixo; dia atual ainda aberto). Os resumos locais até 22:04 somam 4.635 páginas, diferença numérica de seis (0,129%), sem tratar páginas como contador individual HTTP nem atribuir artificialmente o residual a retries. O histórico Git confirmou a correção incremental de usuários em `a602b75`, registrado em 08/09 às 21:48:34; o pacote foi gerado às 20:24:11 e já estava em execução desde 21:06:50 BRT. O percentual refere-se somente a GraphQL; os relatórios analíticos são consumo adicional. Evidências e limites atualizados em `docs/diagnostico-consumo-esl-2026-09-09.md`. Nenhuma alteração operacional ou chamada remota foi realizada nesta conferência.

- Conferência numérica de consumo ESL em 09/09/2026: para os dias 05/09 e 08/09, a aproximação por marcos de 50 páginas do extrator resulta em 27.550 e 21.850, contra 28.006 e 22.342 chamadas `/graphql` no painel fornecido; diferenças de 456 (1,63%) e 492 (2,20%). Trata-se de compatibilidade de volume, não de contagem individual nem de atribuição comprovada de 98% das chamadas. A nova leitura dos resumos de 09/09, até 22:04, soma 4.635 páginas (60 usuários, 837 coletas e 3.738 fretes), ainda sem total correspondente do painel e sem fechamento do dia. Método e limitações adicionados em `docs/diagnostico-consumo-esl-2026-09-09.md`, com JSON/CSV de evidência em `target/unit-tests/conciliacao-consumo-esl-20260909.*`. Esta conferência adicional usou somente arquivos locais e não alterou runtime, configurações nem banco.

- Diagnóstico somente leitura de consumo ESL em 09/09/2026: o painel de 05/09 e 08/09 concentra o volume do usuário API Desenvolvedor em `/graphql`, rota usada pelo processo separado `ETL-EXTRACAO-DADOS-LOOP`, ausente no cliente/JAR do Satélite. Os logs do extrator mostram grandes varreduras de usuários com limite de 2.000 páginas e alertas de threads após timeout. O worker Vedacit está com `VEDACIT_SFTP_RECEIPT_ONLY=true`, API passiva/scheduler geral desligados; a auditoria registra 43 ciclos e 77 envios SFTP em 08/09, sem telemetria ESL no período. A telemetria isoladamente não prova ausência de chamadas. O extrator atual iniciou em 08/09 às 21:06; em 09/09, 17 resumos de usuários somam 53 páginas, indicando redução após a atualização. Detalhes, limites de atribuição e evidências em `docs/diagnostico-consumo-esl-2026-09-09.md`. Nenhum fallback, processo, credencial ou configuração foi alterado; somente agregados de `SATELITE_TMS_AUDITORIA` foram consultados, sem escrita SQL ou novas chamadas ESL.

- Revisão ampliada de 09/09/2026: `scripts/testar_com_cobertura.ps1` reproduz a compilação/teste Java 17 e JaCoCo 0.8.15 em saída isolada, com Maven offline, fontes SOAP locais e bloqueio de rede na JVM de teste. Resultado final em `target/unit-tests/coverage-20260909-193713-449`: 300 testes, 297 aprovados, zero falhas/erros e 3 manuais desabilitados, distribuídos em 50 classes de teste. Cobertura própria: classes 85,89%, métodos 75,62%, linhas 67,88% e desvios 53,54%; baseline anterior de linhas/desvios era 57,65%/42,47%. Relatórios separados incluem código próprio e SOAP gerado, além de inventário de lacunas por classe. Detalhes e limites em `docs/validacao-2026-09-09.md`; cobertura não comprova uso de todas as classes no processo operacional nem validade do SQL/serviços remotos.
- A revisão reproduziu e corrigiu regressão de cursor ESL antes da persistência, ordem SELIA baseada indevidamente em `created_at`, sucesso indevido do worker SFTP quando há erros ou falha de auditoria, e conexão marcada como falha após autenticação bem-sucedida. SELIA ordena a página por `occurrence_at`, preservando a referência `created_at` usada no filtro retroativo; itens fora da janela não ocultam outros itens elegíveis da mesma página. Não houve alteração de DePara, endpoints, toggles ou política de reenvio.
- `VedacitSftpClient` verifica estabilidade em todos os caminhos de leitura, rejeita links e tipos não regulares, verifica diretórios com `lstat` e interrompe downloads que excedam o tamanho anunciado/configurado. A correlação de XML usa `utils/CteXmlValidator` para verificar `infCte/@Id` e NF-e referenciada no namespace fiscal, bloqueando DTD/entidades externas. Os testes cobrem crescimento/truncamento/mudança do arquivo, links, uploads recentes e chaves apenas em comentários; o validador também aceitou uma amostra real lida em memória do SFTP. Nenhuma classe SOAP gerada foi editada manualmente.
- Conferência externa autorizada somente de leitura em 09/09/2026: perfil SFTP Vedacit com 3.085 comprovantes, 3.048 nomes reconhecidos/2.969 NF-es distintas e 37 nomes fora do padrão; 1.488 XMLs. Amostra XML de 9.001 bytes correlacionou exatamente um comprovante por CT-e/NF-e; não foram gravados bytes fiscais. WSDLs oficiais de CT-e/NF-e/ocorrências retornaram 200; `IntegracaoDocumentoComplementar` está no contrato atual e ausente no WSDL local de ocorrências. O manual PPG fornecido confirma os endpoints PHP atuais, divergindo do blueprint interno; a documentação pública atual Intelipost foi usada para conferir cronologia/identificação de eventos. Oito GETs ESL limitados retornaram sete 500 e um 429; as consultas foram encerradas sem inferir mudanças nos eventos 1/110/114. Não houve SQL, POST de integração, mudança remota, geração SOAP remota ou alteração de runtime; JAR operacional permanece com SHA-256 `A77DB9AD8428E5B47553955D0534EB3F83B7511B5E9404A2EB17BE8EEB937751`.
- A validação unitária de 09/09/2026 recompilou o código e os 38 arquivos de teste com Java 17 em `target/unit-tests/run-20260909-184122`, usando Maven offline e goals diretos, sem executar o ciclo de limpeza/geração SOAP remota. As fontes SOAP já geradas foram copiadas sem edição para a saída isolada. O Surefire usou diretório de trabalho próprio, variáveis operacionais removidas apenas do processo de teste e bloqueio de conexões/listeners na JVM de teste; nenhum serviço operacional, SQL Server, ESL, SFTP ou envio SOAP foi acionado.
- `OrquestradorEtlServiceTest` usa a política ESL real com temporizações zeradas, telemetria mockada e verificações de `executarComTelemetria`; foi removido o mock legado de `executar` que impedia a execução dos callbacks. `SateliteApplicationTests` desabilita a importação do `.env`, informa URLs/caminhos SFTP fictícios e verifica ausência de interação nos quatro clientes REST mockados. O teste de fallback de ocorrências Vedacit intercepta a construção do proxy SOAP e valida contratos, endpoint e timeouts sem baixar WSDL. Os três testes manuais de imagem/integração permanecem desabilitados na suíte unitária.
- Resultado de 09/09/2026: a primeira rodada registrou 183 testes, 150 aprovados, 28 falhas, 3 erros e 2 ignorados, além do teste visual excluído preventivamente. Após corrigir exclusivamente os testes, a rodada final incluiu os 184 testes: 181 aprovados, zero falhas, zero erros e 3 manuais ignorados, com `BUILD SUCCESS`. Evidências em `target/unit-tests/run-20260909-184122/RESULTADO.md` e `surefire-reports-final/`. O JAR operacional conserva SHA-256 `A77DB9AD8428E5B47553955D0534EB3F83B7511B5E9404A2EB17BE8EEB937751`; a aprovação unitária não valida SQL real nem o contrato remoto atual da Vedacit.
- Saídas temporárias de compilação na raiz com o padrão `target-build-*` são ignoradas pelo Git. Em 09/09/2026, a pasta `target-build-sftp-vedacit`, gerada na compilação isolada de 07/09, foi retirada do repositório e preservada na área temporária do Windows após bloqueio da exclusão definitiva pela política automática. O JAR principal continua em `target/satelite-0.0.1-SNAPSHOT.jar`, com SHA-256 `A77DB9AD8428E5B47553955D0534EB3F83B7511B5E9404A2EB17BE8EEB937751`; a limpeza não iniciou nem reiniciou serviços.
- Aplicação middleware/ETL headless: o fluxo principal é acionado por Spring Scheduling em `OrquestradorEtlScheduler`, não por controller web.
- Arquitetura em camadas: `clients` concentra OpenFeign REST; `dto.rodogarcia` modela entrada ESL/Rodogarcia; `dto.ppg` modela saída PPG/OK Entrega; `services.etl`, `services.ppg`, `services.vedacit` concentram regras por domínio; `repositories` acessa auditoria SQL; `models` contém entidades JPA; `utils` guarda lógica pura de imagem/download.
- `SateliteApplication` habilita OpenFeign; `SchedulerConfig` habilita agendamento.
- `OrquestradorEtlService` coordena os destinos PPG, SELIA e Vedacit, respeitando toggles independentes, modo ciclo único e modo retroativo, e delega a cadência contínua de paginação/backoff para os serviços de fluxo e política ESL.
- O destino SELIA/Intelipost possui dois sentidos independentes: o receptor público de Pré Lista de Postagem (PLP) e o envio outbound AddEvents. `SELIA_INTELIPOST_PLP_ENABLED=true` mantém o receptor disponível; `APP_SELIA_ENABLED=true` já está preparado no `.env`, limitado pela whitelist e pendente apenas de recarga controlada do processo.
- O destino SUPPORTE é uma integração REST outbound pelo New Tracking, isolada em `clients/SupporteClient`, `dto.supporte` e `services.supporte/SupporteIntegrationService`. Ele usa Basic Auth configurado, DTOs `record`, cursor e auditoria próprios sob o identificador `SUPPORTE`, sem alterar os fluxos SELIA, PPG ou Vedacit; suas listas de configuração normalizam valores nulos de forma explícita para preservar a análise de nulidade.
- O guia operacional `docs/contexto/guia-postman-selia.md` documenta a PLP recebida, as consultas ESL, a validação do comprovante, o POST AddEvents controlado, as variáveis sem segredos, as dependências externas e os critérios de decisão por resposta HTTP.
- `docs/contexto/selia-homologacao-operacao.md` é o registro operacional definitivo da homologação SELIA/Intelipost, incluindo evidências, falhas corrigidas, variáveis e passagem segura para produção.
- `EtlFluxoDestinoService` encapsula paginação por destino, cursor, whitelists, filtro de ocorrência, detecção de loop de cursor, circuit breaker por falhas de infraestrutura, avanço seguro do cursor e pacing por lote de páginas; no incremental, um cursor persistido tem prioridade e não é combinado com `since`.
- `EtlRegistroService` aplica idempotência por log existente, processa pendências de canhoto, baixa comprovantes da ESL e atualiza o estado por registro.
- O reprocessamento manual de canhotos Vedacit usa exclusivamente os logs com `status_dados=SUCESSO`, `status_canhoto=ERRO_DESTINO` e `chave_cte` existente. Ele consulta o comprovante diretamente pelo CT-e, preserva o XML já confirmado e não depende da ocorrência histórica por `invoice_key`.
- `EtlRepescagemService` executa repescagem de erros definitivos do ciclo e repescagem ativa, sem janela de tempo, para falhas parciais de canhoto Vedacit com dados já integrados.
- O dreno foreground de canhotos Vedacit/SFTP trata uma NF-e por vez, mesmo quando a auditoria possuir logs históricos duplicados, e mantém em memória as NF-es já tentadas no processo para avançar pelos próximos candidatos sem reprocessar o primeiro lote. O inventário separa arquivos SFTP, NF-es distintas e logs candidatos; o monitor só oferece a retentativa `P` quando o runner retorna o código exclusivo de timeout ambíguo.
- A pasta SFTP Vedacit é a fonte única da fila de canhotos: o runner materializa cada documento válido identificado por NF-e/CT-e, processa exclusivamente o comprovante SFTP e nunca consulta a ESL nem reenvia XML de CT-e nesse caminho. `PENDENTE_ENVIO` é apenas o estado transitório enquanto o arquivo aguarda a tentativa normal; após processamento, cada item termina em `SUCESSO`, `PENDENTE_TECNICO`, `TIMEOUT_AMBIGUO`, `BLOQUEADO_ORIGEM` ou `BLOQUEADO_DESTINO`. Timeout permanece fora do retry automático, técnico entra em quarentena pós-fila e bloqueios não são reenviados.
- A seleção SFTP Vedacit normaliza e divide a lista completa de NF-es em blocos de no máximo 500 por instrução SQL. Os resultados de todos os blocos são unidos, ordenados globalmente por `dataProcessamento ASC, id ASC`, desduplicados por NF-e e só então limitados; contagens também são somadas por bloco. Isso mantém cada consulta abaixo do teto de 2.100 parâmetros do SQL Server sem truncar o inventário SFTP.
- Em consulta somente leitura à auditoria em 19/08/2026, há 193 pendências reais de canhoto Vedacit (`PENDENTE_FOTO`): 165 com CT-e e 28 sem CT-e. Os 865 registros `NAO_APLICAVEL` não constituem fila de canhoto. Há 185 erros de canhoto, todos já na terceira ou quarta tentativa: 134 decorrem da repescagem legada que tentou reler ocorrência na ESL apesar de dados SFTP/CT-e já existentes; 41 são recusas de negócio da Vedacit; 7 são HTTP 503 histórico; 2 têm formato de imagem incompatível e 1 é timeout histórico sem CT-e. Nenhum erro é elegível à retentativa automática atual.
- A conciliação somente leitura SFTP × auditoria em 19/08/2026 confirmou 1.353 arquivos e 1.341 NF-es no diretório Vedacit. Por NF-e, 829 já possuem sucesso de canhoto na auditoria, 9 estão como `PENDENTE_FOTO`, 142 têm erro sem sucesso prévio, 2 são `NAO_APLICAVEL` e 359 não possuem nenhum log Vedacit. O monitor atual só alcança as 9 pendências já auditadas; os 359 arquivos sem auditoria não podem ser enviados com segurança sem confirmar primeiro a integração do XML/CT-e e criar a trilha de auditoria correspondente.
- O runner SFTP agora materializa em auditoria técnica mínima cada comprovante válido sem log prévio, preservando somente NF-e, CT-e, caminho relativo, origem, status e tentativas — nunca os bytes. No mesmo ciclo, a fila segue a seleção normal de `PENDENTE_FOTO`, sem ESL. O teste unitário confirma a criação do registro para um documento SFTP desconhecido.
- O inventário SFTP também registra na auditoria técnica, sem bytes e sem inclusão na fila, cada nome inválido, tamanho fora do limite ou upload ainda na janela `SFTP_RODOGARCIA_STABLE_FOR_MS`. A identidade é o caminho relativo, para que a mesma evidência seja atualizada de modo idempotente com classificação `PENDENTE_TECNICO` e motivo sanitizável.
- A migration V11 adiciona classificação operacional do canhoto e data de classificação à auditoria. Novas tentativas SFTP passam a classificar sucesso, pendência, erro técnico, timeout ambíguo, bloqueio de origem ou bloqueio de destino sem depender do texto exibido no console; o classificador unitário cobre timeout, recusa Vedacit, imagem inválida e HTTP transitório.
- Em 19/08/2026, a V11 foi aplicada pelo procedimento humano exclusivamente em `SATELITE_TMS_AUDITORIA` e a classificação retroativa confirmou: 165 `PENDENTE_ENVIO`, 141 `PENDENTE_TECNICO`, 1 `TIMEOUT_AMBIGUO`, 41 `BLOQUEADO_DESTINO`, 30 `BLOQUEADO_ORIGEM` e 3.652 `SUCESSO`. Não restou erro Vedacit de canhoto sem classificação.
- A revisão do lote SFTP de 17/08/2026 identificou 16 timeouts de leitura e 8 recusas explícitas. Timeout fica classificado como `TIMEOUT_AMBIGUO`, fora de reenvio automático; recusas ficam como `BLOQUEADO_DESTINO`, fora da seleção de retry.
- Quando o dreno SFTP esgota a fila normal sem atingir o limite seguro de erros, ele executa uma rodada separada apenas dos registros classificados `PENDENTE_TECNICO`. Timeouts ambíguos e bloqueios continuam fora dessa rodada; o teste direcionado cobre a seleção exclusiva do erro técnico.
- Em 19/08/2026 foi identificada uma falha de classificação: `VedacitIntegrationService` devolve timeout SOAP como `ResultadoIntegracao.ERRO_DESTINO`, porém o reprocessamento SFTP o marcava indevidamente como pendência comum. `EtlRegistroService` agora classifica esse resultado pelo caminho de erro, preservando `Read timed out` como `TIMEOUT_AMBIGUO`. A V12 corrige retroativamente somente auditorias Vedacit com `status_canhoto=ERRO_DESTINO` e essa evidência técnica; não envia, remove ou altera arquivos SFTP. A quarentena pós-fila agora limita-se por padrão a 10 itens, pausa de 5 minutos e interrompe ao primeiro erro, evitando uma nova sequência agressiva contra a Vedacit.
- O monitor `K` encerra após qualquer lote com erro individual (código 1), em vez de abrir uma nova execução após 30 minutos. Assim a auditoria permanece disponível para classificação/reconciliação antes de qualquer nova chamada SOAP; apenas o encerramento limpo segue para a consulta periódica e apenas o código 3 oferece a retentativa `P` controlada.
- Após esgotar a fila normal, a quarentena SFTP é automática e tenta até 10 itens técnicos, com pausa de cinco minutos entre eles e limite seguro de três erros na mesma execução. Cada falha é reclassificada antes do próximo item; timeout passa para `TIMEOUT_AMBIGUO` e não retorna à fila técnica. A V13 corrige o HTTP 520 histórico como `PENDENTE_TECNICO`, evitando que ele volte à fila normal de arquivos pendentes.
- O dreno pós-fila de `PENDENTE_TECNICO` foi validado em operação com V11 aplicada: ele só iniciou depois de a rodada normal retornar zero candidatos, selecionou exclusivamente itens técnicos do inventário SFTP, aguardou cinco minutos entre eles, continuou após um timeout já reclassificado e preservou o limite seguro de três erros. A seleção é restrita pela classificação `PENDENTE_TECNICO`; `TIMEOUT_AMBIGUO` e bloqueios ficam fora dela.
- Na execução controlada iniciada às 19:19 de 19/08/2026 com o JAR `vedacit-fix`, a fila normal encontrou 11 candidatos: 2 foram enviados e 9 já estavam conciliados. A quarentena iniciou automaticamente com 10 técnicos, pausa de 5 minutos e limite de três erros; os itens 1, 3, 4 e 5 tiveram sucesso, enquanto o item 2 passou a `TIMEOUT_AMBIGUO` após 3 minutos de espera SOAP. A execução foi encerrada manualmente após o item 5, sem iniciar outra instância em paralelo. Consulta somente leitura posterior confirmou V12 e V13 aplicadas: há 35 `TIMEOUT_AMBIGUO`, 35 `PENDENTE_TECNICO`, 40 `BLOQUEADO_DESTINO`, 2 `BLOQUEADO_ORIGEM` e nenhum HTTP 520 restante em `PENDENTE_ENVIO`.
- O fechamento do runner SFTP agora emite resumo ASCII com técnicos, timeouts ambíguos, bloqueios de origem/destino e próximo passo operacional. As contagens são consultadas por classificação persistida da auditoria Vedacit.
- O resumo ASCII do runner também informa `saldo`, calculado pelas NF-es ainda candidatas no inventário SFTP da rodada.
- A cobertura direcionada da operação SFTP Vedacit valida pasta completa, auditoria mínima ausente, NF-e duplicada, sucesso e duplicidade SOAP, falha transitória, timeout ambíguo, recusa definitiva, upload instável e classificação terminal única. Os 34 testes direcionados passaram sem conexão SFTP, ESL, SOAP ou banco.
- O contrato oficial de ocorrências Vedacit possui a operação `IntegracaoDocumentoComplementar`, agora presente também no snapshot local atualizado em 09/09/2026. `VedacitIntegrationService` mantém o WSDL local como primeira tentativa e preserva a recuperação pelo WSDL oficial derivado da URL configurada diante de incompatibilidade de metadados. Os testes cobrem essa recuperação e a criação real do proxy local atualizado; o JAR operacional anterior ainda conserva o snapshot antigo até a implantação humana do candidato.
- O artefato padrão foi recompilado em 20/08/2026 após a correção do contrato SOAP: `target/satelite-0.0.1-SNAPSHOT.jar`, SHA-256 `52C25FA2E2B45570DF8FD7CA31B86EF6F11DD660DAA0398CC5A3CC62863729A7`. O menu `satelite.bat` usará esse JAR padrão; as instâncias Java anteriores nas portas 9090 e 9091 foram encerradas antes da recompilação para liberar o artefato.
- O `satelite.bat` foi normalizado para CRLF em todas as linhas; os rótulos das opções M/N/O/P voltam a ser localizados corretamente pelo `cmd.exe`.
- As opções N e O do menu foram corrigidas para seus fluxos reais: N executa pendências técnicas e O executa o lote SFTP reconciliado. Os reprocessamentos H, J, N e a prévia M não perguntam mais uma quantidade: usam automaticamente o teto seguro configurado (500 para H/N/M e lotes SFTP de 100 com dreno até ociosidade para J/O). A opção P preserva a confirmação e uma tentativa por vez, pois timeout ambíguo pode já ter sido recebido pela Vedacit.
- Os scripts H e N usam explicitamente o Java 17 resolvido por `scripts/_common.bat`; não dependem mais do `java` global, que pode apontar para Java 8 e impedir a inicialização do JAR Spring Boot.
- O artefato anterior de validação controlada é `target/satelite-0.0.1-SNAPSHOT-vedacit.jar`, gerado em 19/08/2026 com o perfil Maven `fat-jar-alternativo`, SHA-256 `B00FF5FF7AE161D18457F0038B2A72C0E2D2E962A6311B73D0156522361C644B`. A correção final foi reempacotada em `target/satelite-0.0.1-SNAPSHOT-vedacit-fix.jar`, SHA-256 `A5A0F12CE449A3C418AA624C3573C696671808DAA830DE5969100E44F60FDEED`; os scripts SFTP priorizam esse novo artefato quando disponível, sem sobrescrever um JAR ainda em execução no Windows.
- Os scripts `reprocessar_canhotos_sftp_vedacit.bat` e `reprocessar_timeouts_sftp_vedacit.bat` priorizam o artefato alternativo Vedacit quando ele existe, evitando que o menu execute o `satelite-sftp-monitor.jar` antigo.
- A repescagem técnica Vedacit tem execução manual no menu `N` e agendamento diário da API às 23:30 BRT quando `APP_NIGHTLY_RETRY_ENABLED=true`. Ela seleciona no máximo 100 logs com até cinco tentativas e somente falhas técnicas de XML/CT-e ou canhoto (401, timeout, conexão e 429/5xx), atualiza o mesmo log de auditoria com o resultado e preserva fora da automação CT-e sem chave e recusas de negócio da Vedacit. Para erros históricos `401` cujo CT-e ficou apenas no URL da mensagem, extrai exclusivamente a chave de 44 dígitos, grava-a no mesmo log e reenvia somente o XML; o worker de Vedacit mantém esse agendamento desabilitado para não concorrer com a API.
- A auditoria de quarentena usa `dbo.tb_log_integracao_quarentena_evento`, vinculada ao log técnico sem duplicar XML, imagens ou dados de domínio. A entrada em quarentena é idempotente e cada tentativa posterior de repescagem registra resultado `SUCESSO`, `PENDENTE` ou `ERRO`, etapa, mensagem sanitizável e data. `GET /api/etl/quarentena/historico` pagina por período e destino, e `GET /api/etl/quarentena/historico/exportacao` transmite o CSV completo do período, sem teto de página. A migration V8 registra somente a entrada das quarentenas ainda abertas; tentativas históricas já sobrescritas antes dela não são inferidas artificialmente.
- `APP_ETL_REPESCAGEM_ENABLED` permite desligar a repescagem somente em ciclos isolados controlados, impedindo que uma validação SELIA reprocesse pendências históricas de outros destinos.
- `EtlEstadoIntegracaoService` centraliza status, tentativas, datas de processamento e conversão para resultado operacional.
- `AuditoriaDataHoraService` consulta o `SYSDATETIME()` do SQL Server para carimbar os registros de `tb_log_integracao`; se a consulta falhar, há fallback explícito para o fuso `America/Sao_Paulo`, sem modificar registros históricos.
- A retenção de logs de arquivo é executada pelo próprio processo Spring Boot online, independente do scheduler ETL: `LogFileRetentionService` verifica somente a pasta local `logs` a cada `APP_LOG_RETENTION_INTERVAL_MS`, mantém no máximo 20 arquivos pelos limites `LOG_RETENTION_MAX_FILES`, `LOG_RETENTION_MAX_AGE_DAYS` e `LOG_RETENTION_MAX_TOTAL_MB`, e remove também subpastas vazias. `scripts/rotacionar_logs.bat` permanece apenas como operação manual excepcional.
- `EtlResilienciaService` encapsula retries por erro transitório, backoff e limite de tentativas.
- `EslRequestPolicyService` impõe intervalo mínimo entre chamadas ESL, trata HTTP 429 com backoff bloqueante e retry transparente da mesma chamada, trata HTTP 5xx/falhas de transporte como transitórias e controla cooldown para consultas por período.
- A proposta de fonte SFTP complementar da Rodogarcia está documentada em `docs/blueprint/origem-sftp-rodogarcia.md`: SFTP será candidato somente para XML CT-e e comprovante/POD, com ESL preservada para ocorrências e fallback controlado. A conta compartilhada foi registrada exclusivamente no `.env`, permanece desabilitada e exige `SFTP_RODOGARCIA_CLIENT_PATH` explícito por cliente; o processo não poderá listar a raiz nem alternar subpastas. A automação usará cliente SFTP Java nativo com host key pinning, validação de arquivo estável e idempotência existente; FileZilla não participa do runtime.
- Em 14/08/2026, o primeiro caminho configurado foi `/g_rodogarcia/VEDACIT`. Três sondagens independentes sem autenticação apresentaram o mesmo conjunto de chaves RSA, ECDSA e ED25519; por decisão operacional explícita, a chave ED25519 observada foi aceita por confiança inicial (TOFU) e fixada somente no `.env` em `SFTP_RODOGARCIA_HOST_KEY_SHA256`. A automação permanece desabilitada e nenhuma listagem autenticada, download, envio ou alteração remota foi feita.
- A Rodogarcia informou que a integração ESL → SFTP já deposita comprovantes automaticamente. A estrutura Vedacit observada é `/g_rodogarcia/VEDACIT/xml` para XML e `/g_rodogarcia/VEDACIT/comprovantes` para PODs. A amostra visual de comprovantes indica nomes no formato `<referencia>_<chave_cte_44>_<chave_nfe_44>.<extensao>`; a chave CT-e (modelo 57) e a NF-e (modelo 55) permitem correlação direta, mas o padrão permanece hipótese até a leitura controlada e validação do conteúdo.
- Em 14/08/2026, a leitura SFTP autenticada e somente de metadados confirmou `xml` vazia e `comprovantes` com 9 arquivos, 2.996.538 B no total (4 JPEG, 3 JPG, 1 JFIF e 1 PDF). Os 9 comprovantes obedecem ao padrão `<referencia>_<chave_cte_44>_<chave_nfe_44>.<extensao>`. Não houve download, envio à Vedacit, chamada ESL nem alteração no SFTP. A fonte de comprovantes está comprovada; a fonte SFTP de XML ainda depende de haver e validar uma amostra real.
- Ainda em 14/08/2026, após a publicação de uma amostra, o Satélite realizou download temporário e descarte local de um XML de 9.004 B, sem persistência. O XML é bem-formado, possui `infCte` com chave CT-e válida e uma chave NF-e; o cruzamento interno encontrou exatamente um comprovante com o mesmo par CT-e/NF-e. O contrato de leitura/correlação SFTP para Vedacit está validado. Não houve chamada ESL, banco, envio SOAP ou alteração remota; falta somente implementar o adaptador e autorizar uma integração outbound única e controlada.
- Em 14/08/2026 foi criada a base isolada `services/origem/sftp/vedacit`: `VedacitSftpDocumentSource` define a fronteira exclusiva de documentos Vedacit, `VedacitSftpDocument` carrega o resultado tipado e `VedacitSftpPathPolicy` bloqueia caminhos fora da base, constrói somente `xml`/`comprovantes` e correlaciona comprovante por CT-e/NF-e com extensões permitidas. A compilação Java isolada passou. O teste Maven direcionado não foi usado como evidência porque o ciclo legada de geração SOAP/WSDL não encerrou no limite local; não houve conexão SFTP, ESL, SOAP ou banco nesta validação. Ainda faltam cliente SFTP com host key pinning, leitura de bytes e conexão do contrato ao fluxo Vedacit.
- O cliente read-only `VedacitSftpClient` foi adicionado com SSHJ, autenticação por senha exclusiva do `.env`, host key pinning SHA-256 via `FingerprintVerifier.getInstance`, bloqueio de caminho, limite de tamanho e leitura de XML/POD sem upload, renomeação ou remoção. Ele permanece desabilitado e ainda não é chamado pelo fluxo Vedacit; a compilação completa Maven excedeu o limite por causa da geração SOAP/WSDL legada, portanto a integração runtime não está validada.
- A compilação isolada do `VedacitSftpClient` com as dependências Spring e SSHJ passou após corrigir a API pública de `FingerprintVerifier`. O teste local `VedacitSftpClientTest` cobre o modo desabilitado: a origem devolve vazio sem abrir conexão. Não iniciar processo nem ativar `SFTP_RODOGARCIA_ENABLED` antes de ligar o cliente ao fluxo e validar essa integração.
- O fluxo de XML CT-e em `VedacitIntegrationService` agora consulta `VedacitSftpDocumentSource` primeiro e só aceita bytes que contenham as chaves CT-e e NF-e esperadas; SFTP vazio, inválido ou indisponível usa a ESL como fallback. O envio SOAP e os status existentes permanecem inalterados. A integração de canhoto SFTP ainda é pendente: o fluxo atual de canhoto recebe URL ESL e precisa de adaptação específica para bytes locais antes de ser habilitado.
- O `VedacitSftpClient` agora percorre os XMLs candidatos e só devolve o arquivo cujo conteúdo contém o CT-e e a NF-e requeridos; também descarta leitura cujo tamanho final não corresponda ao metadado remoto. Isso impede que o primeiro XML da pasta seja usado indevidamente.
- A leitura SFTP reconsulta tamanho e data de modificação após o download; se qualquer metadado mudar, o documento é descartado como upload ainda instável. A origem segue desabilitada e nenhum serviço foi iniciado.
- O canhoto Vedacit agora prefere bytes SFTP correlatos por CT-e/NF-e e só baixa a URL da ESL quando o SFTP não retornar documento. A compressão e o SOAP existentes recebem os mesmos bytes e permanecem inalterados. A origem continua desabilitada; falta somente teste local mockado e validação final antes de ativação humana.
- Em 14/08/2026, com o Java 17 portátil do projeto, os três testes locais da nova origem SFTP passaram: bloqueio de diretório fora da base, correlação CT-e/NF-e/extensão do comprovante e garantia de que o cliente desabilitado não abre conexão. As classes SFTP e o fluxo Vedacit foram compilados com Java 17 usando o classpath Maven local. Nenhum serviço, SFTP, ESL, SOAP ou banco foi acionado.
- A tentativa de ciclo isolado Vedacit em 14/08/2026 encerrou antes de executar qualquer consulta ou envio: a validação JPA bloqueou a inicialização porque `dbo.tb_esl_request_telemetria` da migration V9 não existe ainda em `SATELITE_TMS_AUDITORIA`. O SFTP foi imediatamente retornado a desabilitado no `.env`. Aplicar a migration V9 pelo procedimento humano controlado é pré-requisito para qualquer novo ciclo.
- Após a aplicação humana da V9, a inicialização ultrapassou a validação JPA e revelou o bloqueio independente da PLP: `SeliaPlpRateLimitService` possuía dois construtores públicos sem um construtor de injeção explícito. O construtor configurado recebeu `@Autowired`; `SateliteApplicationTests` passou com os repositórios de auditoria/telemetria/quarentena mockados, comprovando o carregamento do contexto sem abrir servidor, acessar banco ou chamar integração externa.
- O fluxo documental Vedacit está alinhado como `ocorrência ESL (metadados) -> SFTP do cliente -> SOAP`, preservando a ESL como fonte de ocorrências. Para XML do evento `110`, o SFTP é consultado antes da ESL e valida CT-e/NF-e no conteúdo; ausência ou falha do SFTP usa o XML ESL. Para POD do evento `1`, `EtlRegistroService` não consulta mais a ESL antecipadamente: `VedacitIntegrationService` tenta primeiro o SFTP e, apenas se ele estiver vazio, inválido ou indisponível, consulta a ESL com telemetria e baixa a URL retornada. Sem documento nas duas fontes, mantém `PENDENTE_FOTO`.
- Quando o XML já estiver marcado como `PENDENTE_ORIGEM` por indisponibilidade anterior da ESL, a ativação explícita de `SFTP_RODOGARCIA_ENABLED=true` autoriza uma reavaliação controlada pela nova fonte; com o SFTP desligado, a proteção de não repetir a consulta ESL permanece inalterada.
- A opção de ciclo único foreground mantém scheduler, repescagem, PPG, SELIA e SUPPORTE desligados e agora força `VEDACIT_NFE_WHITELIST_ENABLED=true`. O operador deve preencher a whitelist com exclusivamente a NF-e da amostra e ativar o SFTP somente para o disparo autorizado; o processo foreground usa `target/classes` recém-compilado e não substitui o JAR usado pelo PM2.
- A validação local com Java 17 compilou as classes alteradas. Passaram o contexto Spring e 18 testes direcionados de rate limit, política SFTP e Vedacit, incluindo XML SFTP sem ESL, POD SFTP sem ESL, falha SFTP de POD com fallback ESL e a criação local das portas documentais `ICTe`/`INFe`. O teste legado de criação da porta `IOcorrencias` segue falhando pela divergência já conhecida entre WSDL local e classe SOAP gerada; essa porta não participa do teste documental atual.
- Em 14/08/2026, o filtro de tamanho da PLP foi atualizado de `HttpStatus.PAYLOAD_TOO_LARGE` para `HttpStatus.CONTENT_TOO_LARGE`, equivalente HTTP 413 sem API depreciada; o import Mockito não usado no teste Vedacit foi removido.
- Em 14/08/2026, o ciclo foreground Vedacit isolado foi concluído com evidência real. Para a amostra SFTP (`SHA-256 a9397593b25b7c435925b70d8f84b5e0af9c7fe8ec08212791f729c7ef2a79ba`), a ESL retornou o evento `110` e o Satélite leu o XML pelo SFTP às 18:25:24, enviando-o à Vedacit com sucesso às 18:25:27; no evento `1`, leu o canhoto pelo SFTP às 18:25:34 e a Vedacit confirmou o envio às 18:26:19. A auditoria `SATELITE_TMS_AUDITORIA.tb_log_integracao` registrou `id=7937`, `occurrence_id=302356094`, `ENVIADO`, `status_dados=SUCESSO` e `status_canhoto=SUCESSO`. PPG, SELIA, SUPPORTE e repescagens ficaram desabilitados. Ao término, a whitelist original e `SFTP_RODOGARCIA_ENABLED=false` foram restaurados no `.env`.
- O launcher foreground agora detecta o classpath extraído desatualizado (sem `sshj`) e usa o Fat JAR recém-empacotado; ele também define `APP_ETL_PENDENCIAS_ENABLED=false`. Esse novo toggle conserva o padrão `true` nos demais modos e impede que uma validação pontual reprocesse pendências históricas fora da whitelist.
- O processamento produtivo de canhotos SFTP Vedacit possui agora um runner foreground exclusivo, acessível no menu pela opção `J` e pelo script `scripts/reprocessar_canhotos_sftp_vedacit.bat`. Antes de selecionar uma NF, ele valida conectividade, host key, autenticação e acesso somente de metadados à pasta de comprovantes; qualquer indisponibilidade encerra a rodada com a causa técnica sanitizada, sem alterar pendências ou abrir uma conexão por NF. Ele seleciona apenas registros auditados como `VEDACIT`, `status_dados=SUCESSO`, `status_canhoto=PENDENTE_FOTO` e com NF-e/CT-e válidos; não reenvia XML, não executa outros destinos, não aciona repescagens e exige SFTP configurado com fallback ESL explicitamente bloqueado. Cada execução aceita de 1 a 100 itens (padrão 10), espera 1 s entre eles e grava `canhoto_origem=SFTP` somente após sucesso. A correlação aceita PDF, JPG, JPEG, JFIF e PNG; todos os formatos de imagem são normalizados para JPEG antes do SOAP. Itens sem arquivo ou sem as duas chaves corretas no nome permanecem `PENDENTE_FOTO`, permitindo retomada idempotente no lote seguinte. A opção `K` e `scripts/monitorar_canhotos_sftp_vedacit.bat` drenam lotes sequenciais de até 100 itens enquanto cada lote completo confirmar canhotos: aguardam 30 s entre lotes ativos e só aplicam o intervalo configurável de 1 a 720 minutos (30 por padrão) quando os candidatos se esgotarem, vier menos de 100 candidatos ou ocorrer erro. Há limite defensivo de 50 rodadas por dreno (5.000 itens) e máximo configurável de 100; ao atingi-lo o monitor retoma na consulta posterior. Assim, não há espera de 30 minutos entre os lotes de uma fila grande nem repetição cega de pendências sem arquivo. O monitor fica sob controle explícito do operador por `Ctrl+C`, interrompe diante de erro técnico e não executa outros fluxos. Os prompts usam o valor digitado e somente aplicam o padrão quando o operador pressiona Enter em branco.
- Em 17/08/2026, a investigação somente leitura confirmou que a correlação estrita atual `NF-e + CT-e` é tecnicamente íntegra, porém incompleta para uma NF-e com múltiplos CT-es. No caso `NF-e 35260760642774001209550010002263491221491453`, a ESL retornou o XML do CT-e originalmente auditado `35260760960473000758570030000506151944330916` com `tpCTe=1` (complementar) e referência explícita ao CT-e `35260760960473000758570030000491761607851207`; o XML deste último retornou `tpCTe=0` (transporte normal), contém a mesma NF-e e corresponde ao CT-e presente no arquivo SFTP. Não houve envio SOAP, alteração no SFTP nem escrita no banco nesta investigação. O XML ESL foi confirmado pelo gestor como fonte oficial de classificação do documento.
- Regra Vedacit confirmada pelo gestor em 17/08/2026 para a reconciliação de canhotos SFTP: o CT-e indicado no nome do arquivo FTP é o CT-e efetivo do comprovante e prevalece quando houver mais de um CT-e de transporte para a mesma NF-e; para CT-e complementar, usar o CT-e de transporte indicado no FTP; para reentrega, usar o CT-e de reentrega indicado no FTP; para devolução, enviar o comprovante se ele existir no FTP, inclusive quando representar recusa anotada pelo destinatário. Após um único envio SOAP bem-sucedido, todos os CT-es comprovadamente relacionados àquela NF-e/canhoto devem ficar com status de canhoto `OK`, sem reenviar a imagem. A auditoria deve preservar o CT-e original da ocorrência, o CT-e efetivamente enviado e o vínculo/motivo de reconciliação; o histórico nunca pode ser sobrescrito. Não é permitido aceitar um arquivo somente pela NF-e, nem marcar CT-es sem relacionamento comprovado pelo XML/reconciliação. A estimativa informada foi 22 complementares, 49 reentregas e 58 devoluções; a soma é 129, embora tenha sido citado total 130, devendo a prévia apontar e conciliar a divergência antes do envio em massa.
- A reconciliação Vedacit foi implementada desligada por padrão (`VEDACIT_SFTP_RECONCILIATION_ENABLED=false`). Para correlação exata, mantém o comportamento atual sem consultar ESL. Sem par exato, lê os comprovantes SFTP válidos da NF-e e consulta o XML oficial ESL somente para classificar o CT-e original: complementar (`tpCTe=1`) só é resolvido quando o XML referencia explicitamente o CT-e do arquivo SFTP; sem prova ou com múltiplos candidatos, permanece pendente. Reentregas/devoluções com arquivo de mesmo CT-e continuam elegíveis pela correlação exata. A V10 adiciona os campos auditáveis `canhoto_chave_cte_efetiva`, `canhoto_reconciliacao_tipo` e `canhoto_reconciliacao_motivo`; após sucesso, logs Vedacit da mesma NF-e com XML já confirmado recebem `SUCESSO` de canhoto, sem alterar sua `chave_cte` original e sem novo SOAP. A opção `M` executa prévia foreground sem SOAP nem escrita; a `O`, após confirmação explícita, ativa o lote reconciliado. Os testes locais da decisão complementar, correlação exata, ambiguidade, formatos SFTP e normalização de imagem passaram; o JAR foi empacotado com essas classes.
- Em 17/08/2026, os avisos estáticos de nulidade nos streams de recuperação dirigida, SFTP, SELIA e reconciliação Vedacit foram eliminados por guardas explícitos, sem mudar regras de negócio; a validação de extensão SFTP agora retorna imediatamente para nome nulo. Os testes direcionados `VedacitCteCanhotoReconciliationServiceTest`, `VedacitSftpPathPolicyTest`, `RecuperacaoDirigidaVedacitRunnerTest` e `SeliaIntegrationServiceTest` passaram.
- A primeira prévia produtiva da opção `M` em 17/08/2026 concluiu sem SOAP nem escrita: os 10 primeiros logs elegíveis não possuíam comprovante SFTP válido para suas NF-es, resultando em 0 elegíveis. A reconciliação foi ajustada para encerrar imediatamente nessa condição, sem consultar XML ESL; assim, a prévia em massa não consome a ESL para pendências que não têm qualquer candidato no FTP. O teste direcionado cobre essa garantia e o JAR foi reempacotado.
- Após a prévia, o monitor `K` foi corrigido para inventariar uma vez por rodada os metadados dos comprovantes SFTP e selecionar na auditoria somente pendências Vedacit cuja NF-e esteja nesse inventário. Ele ativa explicitamente a reconciliação de CT-e; assim não fica retido nos registros mais antigos sem arquivo nem testa novamente a ESL nesses casos. O inventário é somente leitura e não baixa os arquivos até o candidato ser selecionado para processamento. `EtlRepescagemServiceTest`, `VedacitCteCanhotoReconciliationServiceTest` e `VedacitSftpPathPolicyTest` passaram; o JAR foi reempacotado.
- A rodada produtiva de 17/08/2026 comprovou três envios SOAP reconciliados e revelou que a fila do monitor ainda filtrava somente `PENDENTE_FOTO`: havia 1.473 logs Vedacit com `status_dados=SUCESSO` e `status_canhoto=NAO_APLICAVEL`, fora da fila apesar de a operação desejar processar seus comprovantes SFTP. O lote reconciliado passou a incluir, exclusivamente quando a NF-e constar do inventário SFTP, os estados `PENDENTE_FOTO` e `NAO_APLICAVEL`; a trava por NF-e já enviada continua impedindo SOAP duplicado. O JAR foi reempacotado após a correção; interromper monitores iniciados com artefato anterior antes de reiniciar o `K`.
- Cada chamada SOAP Vedacit possui também `VEDACIT_SOAP_INVOCATION_TIMEOUT_MS` (210 s por padrão), prazo total independente dos timeouts de conexão/leitura do Metro. Se uma escrita de rede ficar bloqueada, a rodada recebe erro controlado em vez de permanecer travada; a tarefa daemon remanescente não impede o processo foreground de encerrar. No lote SFTP exclusivo, a mensagem de pendência informa explicitamente que a ESL não foi consultada.
- Se um processo foreground anterior mantiver o JAR padrão bloqueado pelo Windows, o lote SFTP prefere o Fat JAR alternativo `target/satelite-sftp-monitor.jar`, gerado com o mesmo projeto exclusivamente para esse fluxo. A execução nova continua proibida enquanto houver processo Vedacit anterior ativo; ele deve ser encerrado pelo operador com a permissão do processo original antes da retomada.
- Em 14/08/2026, a auditoria somente leitura encontrou 133 pendências Vedacit elegíveis ao novo lote (CT-e presente e XML confirmado), 29 pendências sem CT-e e 4 erros de canhoto com CT-e. Os 133 podem ser tratados em dois lotes controlados de 100 e 33; registros sem CT-e permanecem bloqueados até saneamento de origem. O pacote final compilou com sucesso e 8 testes direcionados passaram, cobrindo seleção do lote, marcação de origem SFTP, preferência SFTP, fallback ESL normal e bloqueio da ESL no lote exclusivo. O teste legado da porta SOAP `IOcorrencias` continua incompatível com o WSDL local e está fora desse fluxo documental.
- `IntegracaoAuditoriaQueryRepository` concentra as consultas analíticas de auditoria operacional do Dashboard, incluindo evolução diária e resumo por entidade operacional, sempre com agregação, classificação de status e filtros temporais executados no SQL Server.
- As consultas analíticas de Integrações consideram `VEDACIT`, `PPG` e `SELIA`; os registros técnicos `SELIA_PLP` e `SELIA_PLP_MAP` ficam fora desse conjunto. `MetricaConsolidadaDTO` fornece os rótulos operacionais por destino: PPG/Vedacit usam XML/Dados e Canhoto; SELIA usa AddEvents e POD/Comprovante, sem alterar os campos técnicos de status.
- A auditoria de Integrações e a Quarentena aceitam o parâmetro multivalorado `destino` para PPG, Vedacit e SELIA. A ausência do parâmetro consulta todos; valores fora da lista permitida retornam erro de validação. Métricas, evolução diária, tabela paginada e exportações aplicam a seleção no SQL Server; logs técnicos `SELIA_PLP` e `SELIA_PLP_MAP` não pertencem à lista permitida.
- A quarentena preserva todos os logs para auditoria, mas mostra somente o estado vigente por integração: um erro com sucesso posterior para o mesmo CT-e deixa de ser pendência; sem CT-e, a correlação usa `occurrence_id`. A paginação, contagem, exportação e reprocessamento manual seguem o mesmo critério, sem exclusão física de histórico. Os testes direcionados de repositório e exportação validaram a regra.
- Banco versionado por scripts SQL idempotentes em `database/sql/schema` e `database/sql/migration`; todo script deve usar `USE`, `SET ANSI_NULLS ON` e `SET QUOTED_IDENTIFIER ON`.
- Classes SOAP da Vedacit são geradas em `target/generated-sources/wsimport*`, devem usar as interfaces geradas pelo Maven e não devem ser editadas manualmente.
- Proxies SOAP da Vedacit são criados em `VedacitIntegrationService` pelas classes `Ocorrencias`, `NFe` e `CTe` geradas pelo `jaxws-maven-plugin`, carregando WSDLs locais por `getResource` a partir de `/wsdl/vedacit/...` para funcionar dentro do Fat JAR.
- Os endpoints efetivos da Vedacit continuam configuráveis por `VEDACIT_API_BASE_URL` e são aplicados via `BindingProvider`, substituindo o `soap:address` presente nos WSDLs empacotados.
- `wsimport` usa `-XautoNameResolution` para Ocorrências, NFe e CTe, evitando colisões de classes geradas quando o WSDL oficial expõe tipos com nomes repetidos.
- `build-helper-maven-plugin` registra os três diretórios `target/generated-sources/wsimport*` como fontes Maven, permitindo que IDEs reconheçam os contratos SOAP após a geração.
- As fontes SOAP Vedacit de Ocorrências e NFe foram regeneradas oficialmente pelo `jaxws-maven-plugin` em `target/generated-sources/wsimport*` (299 e 262 fontes, respectivamente). A compilação completa e os testes direcionados de auditoria e Vedacit passaram com 15 testes aprovados, sem alteração do JAR ou do processo PM2 em execução.
- O VS Code não deve filtrar `target` em `java.project.resourceFilters`, pois a extensão Java precisa ler `target/generated-sources/wsimport*`; a pasta pode permanecer oculta somente no Explorer e na busca.
- Logs, auditorias, cursores, quarentenas e estados técnicos de integração usam exclusão lógica obrigatória quando precisarem sair das leituras operacionais. Hard delete/`DELETE FROM`/`TRUNCATE` em rotinas comuns é proibido; use status, `ativo`, `deleted_at`, `arquivado` ou campo equivalente, com filtros explícitos nas consultas de produção.

## Fluxo de Dados e Integrações

- A SELIA comunicou em 23/07/2026 que a modalidade 100% API exige também a Pré Lista de Postagem: a Intelipost faz `POST` para a URL pública da transportadora e o Satélite responde o aceite/rejeição; o AddEvents continua sendo o sentido de saída. O receptor foi implementado com DTOs `record`, validação do header `logistic-provider-api-key`, idempotência por `intelipost_pre_shipment_list`, rejeição total de payload incompleto e correlação técnica mínima NF-e/pedido/volume na auditoria. Uma PLP real de homologação foi aceita, criando um cabeçalho e um mapa técnico `SELIA_PLP_MAP` na auditoria.
- Origem principal: Rodogarcia/ESL Cloud via REST OpenFeign em `RodogarciaClient`.
- Endpoint ESL de ocorrências: `GET ${RODOGARCIA_API_BASE_URL}${RODOGARCIA_CUSTOMER_OCCURRENCES_PATH}`, padrão `/api/customer/invoice_occurrences`, com `Authorization: Bearer <token>`.
- Parâmetros usados na origem: `start` para cursor, `invoice_key` para whitelists/repescagem, `since` para retroativo ou para o primeiro lookback incremental sem cursor, e `occurrence_code=1`.
- Comprovantes ESL: `GET /api/freight_delivery_receipts?cte_key=...`, conforme a documentação ESL validada no Postman.
- O menu `satelite.bat`, opção `H`, executa `scripts/reprocessar_canhotos_vedacit.bat`: o operador informa um limite (padrão seguro: 1) e o processo trata somente canhotos Vedacit em erro. A rotina é headless, não executa retroativo, não reenvia XML e encerra ao final com resumo. O timeout de leitura SOAP Vedacit é de 180 segundos, configurável por `VEDACIT_SOAP_READ_TIMEOUT_MS`.
- O retroativo do menu `R` permite Vedacit, PPG, SELIA ou os três destinos. A opção SELIA inicia um processo isolado, sem scheduler, repescagem genérica ou whitelist de homologação, usando o Java 17 portátil e sem persistir o cursor incremental; assim pode cobrir uma janela histórica sem concorrer com o worker nem avançar seu cursor produtivo.
- XML de CT-e ESL: `GET ${RODOGARCIA_CTE_XML_PATH:/api/ctes}?key=...`, usado quando `VEDACIT_SEND_CTE_XML_ENABLED=true` e com token `RODOGARCIA_MASTER_API_REST`.
- Destinos ativos: `PPG` com token ESL próprio `RODOGARCIA_TOKEN_PPG` e `VEDACIT` com token ESL próprio `RODOGARCIA_TOKEN_VEDACIT`; cada destino mantém cursor independente.
- SELIA é um destino REST implementado no mesmo padrão outbound de PPG e Vedacit: consulta ocorrências ESL com `RODOGARCIA_TOKEN_SELIA` e o comprovante com `RODOGARCIA_TOKEN_SELIA_COMPROVANTE`; exige comprovante e envia AddEvents com ambas as chaves Intelipost. Quando a ESL não trouxer pedido/volume, o serviço usa exclusivamente a correlação da PLP aceita mais recente para a NF-e; sem essa correlação, falha de modo controlado.
- SUPPORTE recebe ocorrências no `POST /V1/tracking/integracao/transp/ocorrencias` com Authorization Basic. O fluxo aceita apenas `occurrence.code == 1` e CT-e cujo CNPJ emitente, extraído da chave de acesso original, esteja em `SUPPORTE_CNPJ_PAGADORES`; esse CNPJ é enviado em `cnpjPagador`. A baixa é enviada imediatamente mesmo sem canhoto; quando o comprovante vier da ESL, ele é convertido para JPEG e enviado em Base64 com prefixo MIME. Sem imagem, o log permanece em `PARCIAL`/`PENDENTE_FOTO` para atualização posterior da mesma ocorrência. As consultas ESL usam `RODOGARCIA_TOKEN_SUPPORTE` para ocorrência e `RODOGARCIA_TOKEN_SUPPORTE_COMPROVANTE` para comprovante; o CNPJ da transportadora configurado é `19109840000468`. O guia sem segredos está em `docs/blueprint/destino_supporte.md`.
- Em 06/08/2026, a releitura da especificação V2 confirmou endpoint, Basic Auth, campos obrigatórios, fluxo sem canhoto e atualização posterior. A SUPPORTE respondeu HTTP 200 ao `OPTIONS` e a um POST técnico `{}` autenticado, retornando o envelope oficial sem NF-e, protocolo ou comprovante; nenhum dado operacional foi integrado. Com os tokens ESL separados, `curl` retornou HTTP 200 para ocorrências (lote vazio) e comprovantes (chave sintética, `data` vazia); a consulta PowerShell de ocorrências retornou 500, divergência já observada para a ESL, sem afetar a validação HTTP equivalente em `curl`.
- As evidências locais para retorno à SUPPORTE estão em `docs/SUPPORT/` (ignorada pelo Git), sem tokens ou credenciais: contrato configurado, payload sintético, comunicação externa, resposta esperada, as seis consultas NOTFIS e pendências explícitas. A validação anterior aprovou 34 testes; após separar os tokens ESL, os 7 testes direcionados SUPPORTE também foram aprovados, incluindo a seleção do token exclusivo de comprovante.
- A ocorrência fictícia de homologação já está pronta na ESL: código `1`, chave NF-e, CT-e e comprovante acessível pela rota oficial. A whitelist SELIA está restrita exclusivamente a essa NF-e; `APP_SELIA_ENABLED=true` está aplicado no `.env` e foi carregado na recarga controlada de 28/07/2026.
- A PLP reenviada do pedido `TESTE2407` foi aceita e criou a correlação com a mesma NF-e da ocorrência ESL. As duas entradas obrigatórias para o envio outbound — ocorrência ESL e PLP — estão vinculadas; nenhum AddEvents foi enviado ainda.
- A PLP do pedido `TESTE2907` foi aceita em 29/07/2026 para a NF `21280` e volume `1`. A pré-validação posterior retornou HTTP `200` na ESL: ocorrência de entrega `code == 1`, CT-e correlacionado e comprovante acessível. A NF-e foi incluída explicitamente na whitelist de homologação, ainda restrita a `TESTE2407` e `TESTE2907`; o envio permanece condicionado à aplicação humana do JAR corrigido e ao disparo único controlado.
- Em 29/07/2026, o `TESTE2907` foi enviado em ciclo único isolado, com PPG, Vedacit, scheduler e repescagem desabilitados. O AddEvents concluiu sem exceção HTTP; a auditoria exclusiva `SATELITE_TMS_AUDITORIA` registrou exatamente um envio `SELIA` (`id=5915`), com dados e canhoto em `SUCESSO`, ocorrência ESL `297983215`, frete `53090948` e carimbo SQL `17:22:45 BRT`. A PLP aceita preserva o vínculo pedido `TESTE2907`/volume `1` para a mesma NF-e.
- A SELIA/Intelipost confirmou visualmente o resultado do `TESTE2907`: macrostatus `Entregue`, microstatus `ENTREGUE NO DESTINO`, código original `01` e mensagem `Entrega Realizada Normalmente`. A homologação está concluída.
- A investigação de leitura de 28-29/07/2026 confirmou configuração SELIA habilitada, whitelist restrita e DEXPARA `01`. A rota de comprovante antes implementada estava divergente da documentação e foi corrigida para `/api/freight_delivery_receipts`; o comprovante SELIA também passou a usar token próprio. As primeiras leituras com `Invoke-WebRequest` retornaram HTTP `500`, mas a reprodução com `curl`, no mesmo formato HTTP do Postman e com os tokens fornecidos, retornou HTTP `200` para ocorrência e comprovante. A pré-validação confirmou ocorrência `code == 1`, NF-e correspondente, CT-e disponível e `image_url` do comprovante. Nenhum `POST` AddEvents foi executado; o fluxo está apto após empacotar e recarregar a correção.
- O processo PM2 `Satelite-API-19090` executa o JAR do Satélite na porta local `19090`. Após encerrar uma instância Java órfã que mantinha a porta ocupada, a execução foi normalizada em 28/07/2026: há uma única Java escutando a porta, o PM2 permanece `online` sem novos reinícios e o endpoint local responde HTTP `405` ao `GET` esperado. O Cloudflare Tunnel publica exclusivamente o caminho `^/api/selia/intelipost/pre-shipment-list$` do hostname `satelite-api.rodogarcia.com.br` para `http://127.0.0.1:19090`; o catch-all do Tunnel retorna HTTP 404 para os demais caminhos. O HTTPS externo também confirmou HTTP `405` para `GET`; em testes anteriores, `401` para `POST` com chave inválida e `400` para `POST` autorizado com payload propositalmente incompleto. Nenhum dado operacional foi gravado nesses testes.
- O `ecosystem.config.js` contém o worker de produção `ETL-SELIA-Worker`, espelhado no padrão de ciclo único do `ETL-Vedacit-Worker`: processo headless, logs próprios, reinício pelo PM2 após 30 minutos e sem scheduler Spring. Ele habilita somente SELIA/AddEvents, desabilita PPG, Vedacit, SUPPORTE e a repescagem genérica, e força a desativação da whitelist SELIA para a leitura incremental produtiva. Em 07/08/2026 foi identificado que o PM2 resolvia o comando genérico `java` para Java 8 (`...Oracle\\Java\\java8path\\java.exe`), incompatível com o runtime Java 17; o worker aponta explicitamente para o Java 17 portátil e inicia `SateliteApplication` com as classes compiladas em `target/classes` e as bibliotecas extraídas do Fat JAR em `target/selia-worker-inspect/BOOT-INF/lib`, preservando os demais processos que mantêm o JAR compartilhado bloqueado.
- Na pré-validação somente de leitura de 07/08/2026, a ESL SELIA retornou uma única página com três ocorrências `code == 1` desde 04/08/2026, correspondentes às NFs `5322`, `5307` e `5310` presentes na tela operacional. As três possuem CT-e e comprovante disponível por URL assinada na rota oficial; como a ESL não traz pedido/volume nelas, a auditoria exclusiva `SATELITE_TMS_AUDITORIA` confirmou uma correlação `SELIA_PLP_MAP` aceita e completa para cada NF. Não existe log outbound `SELIA` prévio para essas três ocorrências, portanto permanecem elegíveis ao primeiro envio idempotente. Os demais documentos da tela, em `Pendente`, `Em trânsito` ou `Standby`, não retornaram no filtro obrigatório `occurrence_code=1` e não serão enviados como entrega realizada.
- O primeiro ciclo real do worker SELIA, em 07/08/2026, iniciou corretamente com Java 17 e terminou com código `0`; o estado PM2 `waiting restart` é esperado entre ciclos únicos. A ESL respondeu vazia quando o cursor SELIA persistido `297983215` foi combinado com o lookback `since`, mas retornou as três ocorrências prontas com somente `start`. `EtlFluxoDestinoService` foi corrigido e o teste de orquestração cobre a prioridade do cursor. O ciclo produtivo seguinte foi concluído sem falhas: as ocorrências `300258586`/NF 5322, `300293059`/NF 5307 e `300368674`/NF 5310 tiveram comprovante obtido, AddEvents Intelipost com `HTTP 200 OK` e auditoria `ENVIADO`, `status_dados=SUCESSO`, `status_canhoto=SUCESSO`; o cursor SELIA passou para `300368674`. A configuração foi salva no PM2.
- A Intelipost documenta consultas somente de leitura para localizar pedido por chave NF-e (`GET /shipment_order/invoice_key/{chave_nfe}`) e volumes pelo pedido (`GET /shipment_order/get_volumes/{pedido_envio}`). Elas seguem fora do runtime e são contingência opcional: ambas retornaram HTTP 401 com a credencial atualmente configurada, mas a PLP aceita passa a suprir a correlação necessária para o rastreamento.
- As credenciais SFTP Intelipost da SELIA permanecem isoladas no `.env`, mas EDI/OCOREN não faz parte do escopo selecionado e não deve ser ativado ou implementado.
- A paginação ESL continua até `data` vazio, encerramento por janela retroativa ou falha não recuperável; `INTEGRATION_MAX_PAGES_PER_CYCLE` define apenas quantas páginas compõem um lote antes da pausa `ETL_PAGINATION_PACING_PAUSE_MS`, sem encerrar o ciclo.
- Destino PPG/OK Entrega: `PpgClient` faz login em `/assets/ws/ws.0.loginapp.php` e envia ocorrência em `/assets/ws/ws.0.ocorrenciaentregacache_api.php`; `PpgAuthService` mantém token em memória por 13 dias.
- Destino Vedacit/MultiTMS: SOAP em `Ocorrencias.svc`, `NFe.svc` e `CTe.svc`, com token em header SOAP, timeouts configuráveis, WSDLs locais carregados do classpath e endpoints finais definidos por `VEDACIT_API_BASE_URL`.
- Canhotos PPG: imagem baixada da ESL, convertida para RGB/JPEG, recortada, redimensionada para `1536x240`, gravada a 150 DPI e enviada com prefixo `data:image/jpeg;base64,`.
- Canhotos Vedacit: imagem ou primeira página de PDF é convertida/comprimida em JPEG, limite máximo de 400 KB, Base64 bruto sem prefixo MIME.
- Persistência técnica: `dbo.tb_log_integracao` registra ocorrência, chave NFe, frete, cursor, status geral, status de dados, status de canhoto, tentativas, mensagens de erro, payloads e referência de canhoto; `dbo.tb_controle_cursor` armazena cursor por `sistema_destino`.
- Endpoints auxiliares expostos: `/api/auditoria/integracoes-clientes`, sua exportação `/api/auditoria/integracoes-clientes/exportacao`, `/api/auditoria/integracoes-clientes/evolucao-diaria`, `/api/auditoria/integracoes-clientes/resumo-tabelas`, `/api/auditoria/logs/{id}/imagem`, `/api/etl/quarentena/erros`, sua exportação `/api/etl/quarentena/erros/exportacao` e `/api/etl/quarentena/{destino}/reprocessar`. As duas exportações geram CSV por `StreamingResponseBody`; `IntegracaoAuditoriaQueryRepository` aplica filtros/ordenação da auditoria ou a seleção de quarentena diretamente no SQL Server e envia linhas ao stream, sem lista integral na JVM.

## Regras de Negócio Consolidadas

- O fluxo principal só deve processar entrega realizada: `occurrence.code == 1`. Outros códigos são ignorados e auditados.
- Controllers não devem acionar o fluxo principal de integração; endpoints web existem apenas para auditoria, imagem de canhoto e quarentena/reprocessamento manual.
- Ações SQL manuais ou automatizadas devem atingir exclusivamente `SATELITE_TMS_AUDITORIA`; é proibido tocar `ETL_SISTEMA`, `DASHBOARDS` ou qualquer outra database.
- O banco guarda apenas auditoria, status, cursor e erros. Não persistir fisicamente NF completa, imagem de canhoto ou massa operacional de domínio.
- O cursor só avança depois do processamento/auditoria da página. Em erro de registro no modo incremental, o cursor não avança para permitir retry.
- O modo incremental usa `since` calculado como agora menos `ETL_INCREMENTAL_LOOKBACK_HOURS` (padrão 24 horas) somente quando não existe cursor persistido; com cursor, consulta somente `start`, mantendo paginação e avanço seguro sem a combinação incompatível da ESL.
- Em carga retroativa, erros por registro são registrados e a paginação pode avançar em memória para não travar o histórico.
- Em carga retroativa, `RETROACTIVE_MAX_PAGES`/`retroactive.max-pages` não podem ultrapassar `INTEGRATION_MAX_PAGES_PER_CYCLE`; o valor resultante define o tamanho do lote antes de pacing, não um limite terminal do ciclo.
- `INTEGRATION_MAX_PAGES_PER_CYCLE` não encerra mais a paginação; ao atingir o lote configurado, o fluxo pausa por `ETL_PAGINATION_PACING_PAUSE_MS` (padrão 30000 ms) e retoma do cursor em memória/persistido.
- Cada destino tem whitelists independentes de NF-e: `PPG_NFE_WHITELIST_ENABLED/PPG_NFE_WHITELIST` e `VEDACIT_NFE_WHITELIST_ENABLED/VEDACIT_NFE_WHITELIST`.
- Registros com status final `ENVIADO` ou `IGNORADO` não são reenviados automaticamente.
- Status consolidados atuais: `RECEBIDO`, `IGNORADO`, `ENVIADO`, `PARCIAL`, `PENDENTE_FOTO`, `PENDENTE_ORIGEM`, `ERRO_DESTINO`, `SUCESSO` e `NAO_APLICAVEL`.
- PPG exige canhoto disponível; sem imagem, o registro fica `PENDENTE_FOTO` e não envia payload.
- Vedacit pode concluir dados e canhoto separadamente; se canhoto faltar, o registro pode ficar parcial com `status_canhoto=PENDENTE_FOTO`.
- Quando a ESL responde sem XML de CT-e, o Satélite registra `status=PENDENTE_ORIGEM` e `status_dados=PENDENTE_ORIGEM`, preserva o canhoto, não incrementa tentativa nem entra na repescagem técnica. Uma nova emissão do mesmo CT-e é ignorada localmente até ação controlada após a origem disponibilizar o documento.
- Registros Vedacit com `status=ERRO_DESTINO`, `status_dados=SUCESSO`, `status_canhoto=ERRO_DESTINO` e `tentativas_canhoto < 3` são resgatados pela repescagem ativa sem limite de janela; o XML/dados é considerado já integrado e o retry executa somente download, compressão e envio SOAP do canhoto.
- Falhas parciais de canhoto preservam `status_dados=SUCESSO`, incrementam `tentativas_canhoto` a cada nova falha e entram na quarentena natural ao alcançar `tentativas_canhoto >= 3`.
- O dashboard de pendências deve exibir falhas parciais de canhoto ainda retryáveis como `Erro Parcial - Aguarda Retry`, evitando invisibilidade operacional.
- `QuarentenaService` seleciona a primeira mensagem disponível entre erro de dados, canhoto e erro geral diretamente pelos valores recebidos; não mantém sobrecarga local sem chamadas.
- O resumo analítico de integrações para o Dashboard deve agrupar no SQL Server por entidade operacional derivada de `tb_log_integracao`: `sistema_destino - XML/Dados` e `sistema_destino - Canhoto`. Como o banco de auditoria não persiste uma coluna física de tabela de negócio (`tabela_codigo`/`tabela_busca`), não se deve inventar essa dimensão no Dashboard nem agregar os logs em memória.
- Duplicidade retornada por PPG ou Vedacit é conciliada como sucesso, não como erro fatal.
- O runtime Vedacit não deve ler WSDL por caminho de filesystem como `src/main/resources`; a leitura deve ser por classpath (`getResource`) e falhar explicitamente se o WSDL local não estiver empacotado.
- Erros HTTP transitórios ou de infraestrutura incluem 500, 502, 503, 504, timeouts e falhas de transporte; há backoff e limite de tentativas antes de quarentena/bloqueio de retry automático.
- Circuit breaker de infraestrutura abre por falhas consecutivas conforme `ETL_CIRCUIT_BREAKER_TRANSIENT_FAILURE_THRESHOLD`.
- A ESL deve respeitar intervalo mínimo entre requests (`ESL_MIN_INTERVAL_BETWEEN_REQUESTS_MS`, padrão 2000 ms); HTTP 429 aplica backoff bloqueante (`ESL_TOO_MANY_REQUESTS_BACKOFF_MS`, padrão 120000 ms) e repete a mesma chamada no máximo por `ESL_TOO_MANY_REQUESTS_MAX_RETRIES` (padrão 2) novas tentativas. Ao esgotar esse limite, a falha transitória é auditada para reavaliação no próximo ciclo, sem teto mensal de consumo.
- Consultas por período usam cooldown: até 30 dias sem cooldown adicional, 31 a 183 dias com 1 hora, acima disso com 12 horas.
- Toda credencial, URL e token deve vir de `.env`, `application.properties` ou variável de ambiente; não pode haver segredo hardcoded.
- SELIA usa AddEvents como saída e PLP como entrada obrigatória na modalidade 100% API. A documentação de PLP demonstra que `order_number` e `shipment_order_volume_number` podem ter valores diferentes; o DTO ESL tolera `order_number` e `volume_number` na ocorrência ou no frete e, quando eles não vierem, o serviço usa somente os pares correlacionados da última PLP aceita para a mesma NF-e. Sem os dois identificadores por uma dessas fontes, falha de modo controlado, sem SFTP, NOTFIS ou inferência.
- O cliente REST SELIA deve enviar `api-key`, `logistic-provider-api-key`, `platform`, `platform-version`, `plugin`, `plugin-version` e `Content-Type: application/json`. O DePara oficial refeito em 13/08 associa a entrega ESL `1` ao código do cliente `1`; os demais códigos aceitos estão exclusivamente em `SELIA_INTELIPOST_EVENT_CODE_MAP`. O comprovante ESL é obrigatório somente na entrega e segue como anexo POD; HTTP `429` respeita `ratelimit-reset` quando a resposta o fornecer, sem quebrar a cronologia de eventos.
- A chave REST de Rastreamento/AddEvents da SELIA foi recebida e está exclusivamente em `SELIA_INTELIPOST_API_KEY` no `.env`. Sua existência evidencia que o REST também é um canal disponível, mas não autoriza chamadas produtivas nem resolve os metadados obrigatórios, o de-para de eventos ou o vínculo pedido/volume.
- A investigação somente de leitura da SELIA está registrada em `docs/blueprint/relatorio-descoberta-selia.md`: a ESL voltou a responder HTTP 200 ao consultar ocorrências com o token SELIA, mas retornou `data` vazia; a consulta temporal subsequente recebeu HTTP 429 sem `ratelimit-reset`. A Intelipost respondeu HTTP 401 ao `GET /info` com as chaves em conjunto ou isoladas. Esses resultados não autorizam POST de sondagem, nem permitem inferir pedido, volume ou DePara.
- A continuação da investigação confirmou por `OPTIONS` que `/tracking/add/events` expõe `POST`, mas uma chamada `GET` nessa URL atingiu outra rota e retornou HTTP 400; isso não valida a autenticação de envio. As consultas oficiais Intelipost por NF-e e por volumes retornaram HTTP 401 com as credenciais configuradas. Nenhum POST AddEvents foi executado.
- A Pré Lista de Postagem Intelipost é um `POST` de entrada da Intelipost para a URL pública da transportadora, autenticado por `logistic-provider-api-key`. O receptor implementado não registra a chave, payload bruto ou dados pessoais; valida toda a lista antes de gravar, responde todos os pedidos/volumes recebidos e grava somente identificadores técnicos de correlação em `tb_log_integracao`. A resposta usa o ID técnico da auditoria como `logistics_provider_shipment_list`, decisão pendente de aceite explícito da SELIA/Intelipost.
- O Satélite não cria cotações, despacho operacional, PLP/NOTFIS, etiquetas, cancelamentos ou webhooks adicionais para SELIA. A única entrada é o receptor obrigatório de Pré Lista, limitado ao aceite/rejeição técnico da PLP; a integração de saída continua restrita à publicação de ocorrências AddEvents com comprovante POD obrigatório. A ausência de comprovante mantém o registro em `PENDENTE_FOTO` para retry.
- A migration `database/sql/migration/V6__selia_plp_auditoria_correlacao.sql` adiciona à auditoria somente `intelipost_pre_shipment_list`, `logistics_provider_shipment_list`, `order_number` e `volume_number`, com índices de busca. Ela foi executada com sucesso exclusivamente em `SATELITE_TMS_AUDITORIA` por `database/subir_database.bat`.
- Os testes de PLP cobrem aceite válido, lista repetida, chave inválida, payload incompleto, ausência de vazamento do segredo e controller MVC; a correlação com dois volumes também é coberta no serviço outbound, sem avisos de segurança de tipos no Java. O JAR foi recompilado e o processo PM2 foi reiniciado antes da validação HTTPS externa.
- A suíte SELIA foi executada com Java 17 portátil usando o classpath de teste já compilado, sem acionar a geração SOAP remota: 7 testes aprovados, cobrindo autenticação do receptor, aceite e idempotência da PLP, rejeições de contrato e envio AddEvents com POD/correlação pedido-volume. O ciclo Maven completo continua dependente dos WSDLs remotos da Vedacit e não deve ser usado como único critério da homologação SELIA.
- A seleção segura de Quarentena foi validada por 16 testes direcionados: ausência, um ou múltiplos destinos, destino inválido, exportação filtrada no SQL e bloqueio da repescagem genérica SELIA. A suíte Maven completa continua dependente dos WSDLs remotos da Vedacit; em 05/08/2026 a geração de CTe retornou uma fonte com comentário não fechado, impedindo a compilação antes da execução dos testes.
- Após a correção da rota, da separação do token de comprovante SELIA e do relógio de auditoria, as classes alteradas foram compiladas com alvo Java 17. O empacotamento Maven completo continua bloqueado pela geração remota dos WSDLs Vedacit; a compilação SELIA não falhou. O substituto `target/satelite-0.0.1-SNAPSHOT-selia-fix.jar` contém as correções de comprovante, filtro de whitelist e hora SQL, e foi validado estruturalmente. Falta somente a troca/reload humano controlado.
- O artefato final de produção foi preparado como `target/satelite-0.0.1-SNAPSHOT-selia-production-ready.jar`, com SHA-256 `DCD49C209035364B8E6BA688A3C592B1D70CF048C60727346E6B4E555DEC5AAB`. A suíte focada SELIA/ETL executou 41 testes, todos aprovados; o processo PM2 atual não foi alterado.
- Em 29/07/2026 foi executado um ciclo SELIA isolado, sem scheduler, PPG ou Vedacit, usando a whitelist exclusiva da NF-e de homologação. A consulta ESL confirmou a ocorrência `1`, o CT-e e o comprovante; o Satélite enviou um único `POST /api/v1/tracking/add/events` para a Intelipost e recebeu HTTP `200 OK`. A auditoria exclusiva `SATELITE_TMS_AUDITORIA` registrou o evento como `ENVIADO`, com dados e canhoto em `SUCESSO`, para a ocorrência ESL `297184081` e frete `52963210`. A correlação da PLP aceita vincula essa NF-e ao pedido `TESTE2407`, volume `1`; a releitura da auditoria confirmou exatamente um envio, sem duplicidade. O evento usa o código DEXPARA `01`, correspondente a `DELIVERED` / `ENTREGUE NO DESTINO`.
- Em 29/07/2026 foi detectada inconsistência de horário na auditoria: às 17:04 BRT, Windows e SQL Server estavam sincronizados, porém o registro SELIA `5912` constava às 18:29 e registros posteriores de PLP/Vedacit também estavam no futuro. O PM2 força `APP_SCHEDULER_ENABLED=false`; portanto, não existe atraso de 15 minutos ou execução agendada explicando o horário. Até rastrear a origem desses timestamps futuros e obter a confirmação visual da Intelipost, o horário de auditoria não deve ser apresentado como evidência cronológica do envio.
- Durante a validação foi corrigida uma lacuna no fluxo: a whitelist SELIA agora também é aplicada como `invoice_key` na consulta ESL e, quando esse filtro explícito existe, a consulta não combina a janela incremental de 24 horas. Isso permite retestar com segurança uma NF-e homologada mais antiga sem varrer outras notas. A suíte de orquestração foi recompilada com Java 17 portátil e aprovou 29 testes.
- Erros e contingência de cotação Intelipost (peso/CEP/dimensões e tabelas Fallback Tables V2) pertencem à plataforma/ERP de cotação. O Satélite não tem os dados nem endpoint para cotar e não deve calcular frete ou carregar tabelas de fallback no escopo SELIA de rastreamento.
- DTOs REST próprios devem preferir `record`, camelCase no Java e `@JsonProperty` para nomes externos divergentes; DTO raiz deve tolerar campos desconhecidos.
- Fluxos que transformam coleções potencialmente anuláveis devem validar explicitamente os elementos antes de acessar métodos ou campos, mantendo a análise de nulidade da IDE ativa sem avisos mascarados.
- Comparadores de caminhos de arquivo devem usar lambdas tipadas ao encadear chaves de ordenação, preservando a análise de nulidade da IDE.
- Qualquer alteração de banco deve ser script SQL versionado e idempotente; a atualização operacional esperada é executar `database/subir_database.bat`.
- Registros de auditoria e logs devem preservar rastreabilidade histórica; limpezas físicas só podem existir como política técnica excepcional, documentada, versionada, idempotente e restrita ao banco `SATELITE_TMS_AUDITORIA`.
- SELIA pode ser consultada e exportada na Quarentena, mas não pode ser redefinida pelo endpoint manual nem pela repescagem genérica. Uma falha definitiva permanece rastreável até a criação de um fluxo SELIA específico que valide POD, correlação PLP pedido-volume e idempotência AddEvents.
- A auditoria SELIA possui no schema canônico e nas migrations idempotentes os campos de destino, status, tentativas, data e referência de comprovante, além dos índices `IX_tb_log_integracao_auditoria_escopo`, `IX_tb_log_integracao_selia_plp_lista` e `IX_tb_log_integracao_selia_plp_nfe`; não há ajuste estrutural pendente.
- A reciclagem de arquivos de log é independente da auditoria SQL e limitada à pasta local `logs/`: `LogFileRetentionService` executa no próprio processo online e mantém no máximo 20 arquivos, 30 dias e 500 MB, removendo primeiro os mais antigos e apagando subpastas vazias. Ela é ativada por `APP_LOG_RETENTION_ENABLED=true`, roda a cada 15 minutos por padrão e não depende do PM2.
- O worker PM2 de produção SELIA executa somente o fluxo outbound AddEvents em modo incremental. Ele usa `APP_CICLO_UNICO=true` e `APP_SCHEDULER_ENABLED=false`; o PM2 aguarda 30 minutos após cada encerramento antes do próximo ciclo. A execução não usa a whitelist de homologação, mas preserva a idempotência de `tb_log_integracao`, a exigência de comprovante POD e a correlação PLP NF-e/pedido/volume.
- A API passiva do Satélite na porta `19090` e os workers PM2 usam explicitamente o Java 17 portátil do projeto, sem depender do Java global do Windows. Isso mantém o Fat JAR compatível e evita indisponibilidade `503` do proxy de Integrações por falha de inicialização.
- O menu `satelite.bat` e seus auxiliares aceitam Vedacit, PPG, SELIA e SUPPORTE de modo explícito, tanto em loop com API quanto em ciclo único foreground. Os ciclos manuais desligam scheduler e repescagem, preservam a idempotência e usam Java 17 portátil com as classes compiladas quando o runtime local está disponível; SELIA e SUPPORTE iniciam desabilitados nos demais modos, sendo ativados apenas pela opção selecionada. As opções SELIA desabilitam a whitelist de homologação para consultar o cursor produtivo.
- Os loops iniciados pelo menu usam `javaw.exe` quando disponível, com saída redirecionada para `logs/`, para não criar uma janela de terminal que possa ser fechada acidentalmente. Os workers PM2 permanecem a opção supervisionada de operação contínua e também executam ocultos.
- Em 07/08/2026 foi executado pelo menu o ciclo único SELIA/AddEvents com Java 17 e `invoice_key=null`, partindo do cursor produtivo `300368674`. A ESL repetiu somente a última ocorrência já auditada, que foi ignorada pela idempotência; o ciclo concluiu sem falhas e não produziu duplicidade. Os três AddEvents produtivos anteriores permanecem confirmados na auditoria como `ENVIADO`/`SUCESSO`.
- O relatório final de quarentena respeita os destinos efetivamente ativos e selecionados no ciclo. Assim, um disparo isolado SELIA/AddEvents consulta e exibe somente pendências SELIA; erros históricos de Vedacit, PPG ou SUPPORTE não são misturados ao seu resultado.
- O relatório gerencial `docs/relatorio-selia-disparo-2026-08-07.md` consolida a solicitação de validação, o contrato sanitizado da requisição AddEvents, os três envios SELIA confirmados na auditoria, o ciclo idempotente posterior e as 16 NFs distintas da tela que ainda não retornaram como `occurrence.code == 1`.
- Mudança Vedacit implementada em código e ainda pendente de aplicação humana controlada: o XML do CT-e é integrado pelo evento ESL `occurrence.code == 110` (`CTE Emitido`), sem depender da entrega realizada; o canhoto continua condicionado exclusivamente ao evento `occurrence.code == 1`. Há cursor independente `VEDACIT_XML`, idempotência por chave de CT-e e migration `V7__vedacit_xml_por_cte.sql`. A cadência do worker PM2 permanece de 30 minutos. Cancelamentos não exigem ação outbound do Satélite porque a Vedacit remove o CT-e cancelado; uma reemissão integra o XML do novo CT-e. A recuperação controlada inicia em `01/05/2026` no fuso `America/Sao_Paulo` quando `VEDACIT_XML_BACKFILL_ENABLED=true` for definido antes da aplicação humana do novo artefato.
- O XML Vedacit possui retry técnico progressivo e limitado exclusivamente para falhas transitórias: `VEDACIT_XML_RETRY_INITIAL_BACKOFF_MS` (padrão 15 s), `VEDACIT_XML_RETRY_BACKOFF_MULTIPLIER` (2) e `VEDACIT_XML_RETRY_MAX_BACKOFF_MS` (120 s) definem a espera crescente; `INTEGRATION_MAX_RETRY_ATTEMPTS` preserva o teto de três tentativas. Recusas de negócio e CT-e sem chave não entram no retry. Os demais fluxos preservam o backoff fixo existente.
- Em 11/08/2026, a validação isolada com Java 17 executou 30 testes direcionados de `EtlResilienciaServiceTest`, `EtlRepescagemServiceTest`, `OrquestradorEtlServiceTest`, `QuarentenaControllerTest` e `IntegracaoAuditoriaQueryRepositoryTest`, todos aprovados. A cobertura confirma o backoff progressivo do XML Vedacit, limite de tentativas, bloqueio após o limite, cursor `VEDACIT_XML` e evento `110` sem consulta de canhoto, além da repescagem noturna de XML Vedacit, dos filtros de quarentena e da exclusão de recusas de negócio/SELIA da repescagem genérica.
- A verificação somente leitura em 11/08/2026 confirmou que a migration V7 já está aplicada (`chave_cte` e índice `IX_tb_log_integracao_destino_chave_cte`) e que o cursor `VEDACIT_XML` está ativo. O log do worker também confirmou consulta do evento `110` e idempotência de XML já integrado. O artefato local pronto para publicação controlada é `target/satelite-0.0.1-SNAPSHOT-vedacit-recuperacao-dirigida.jar`, SHA-256 `964980EABD758265E813943C284AB66B24F83552199B1AA9356E71EB0A85F09D`; ele contém o retry progressivo e a recuperação dirigida. A substituição do JAR principal exige parada humana, pois a API mantém o arquivo em uso no Windows.
- A consulta somente leitura à API ESL em 11/08/2026, filtrada por `occurrence_code=114`, retornou HTTP 200 e 20 registros cuja descrição é `Comprovante de Entrega Anexado`, todos com CT-e. A evidência é conclusiva para o Satélite: 114 é evento de anexo de POD/canhoto, não emissão de CT-e, e permanece fora do fluxo de XML; não há dependência pendente de retorno formal da ESL.
- A auditoria Vedacit atualizada em 11/08/2026 aponta 28 recusas de digitalização, 13 recusas por canhoto incompatível, 39 registros sem chave CT-e, 2 imagens em formato não suportado e 10 outros erros vigentes. As duas imagens sem formato pertencem a logs históricos sem CT-e, referência, MIME ou URL preservada; não há dado técnico suficiente para reproduzir ou reprocessar com segurança.
- A conciliação somente leitura da base privada `analise_vedacit_comprovantes(MultiTMS).csv` em 11/08/2026 encontrou 5.365 linhas: 2.726 integradas completamente na auditoria, 1.627 com dados/XML integrados e canhoto pendente/erro, 45 com pendência/erro de dados e 967 sem registro na auditoria. Entre as sem auditoria, 827 estão marcadas como disponíveis na ESL. Uma consulta dirigida de amostra retornou para a mesma NF-e os eventos `110` (`CTE Emitido`, com chave CT-e) e `1` (`Entrega Realizada Normalmente`), comprovando que a recuperação por NF-e é tecnicamente viável, mas a execução em massa continua dependente de autorização operacional por gerar envios à Vedacit.
- `RecuperacaoDirigidaVedacitRunner` é uma rotina foreground, ativada somente por `vedacit.recovery.enabled=true`. Ela recebe um TXT local de NF-es, aceita no máximo 200 chaves por execução, consulta cada chave somente com `invoice_key` e ocorrência `110`, não altera cursor, não consulta/envia canhoto e delega o XML à idempotência normal por CT-e. O auxiliar `scripts/recuperar_nfes_vedacit.bat` executa a rotina sem habilitar PPG, SELIA ou SUPPORTE. O teste de leitura de arquivo valida chave de 44 dígitos, remoção de duplicidade e limite do lote.
- `scripts/gerar_lista_recuperacao_vedacit.ps1` cruza a base privada Vedacit com a auditoria exclusiva `SATELITE_TMS_AUDITORIA` em modo leitura e gera o TXT elegível para recuperação. Em 11/08/2026 ele produziu `docs/SECRETO/MULTITMS/nfes_recuperacao_vedacit.txt` com 827 NF-es marcadas como disponíveis na ESL e sem auditoria Vedacit; o arquivo é sigiloso e ignorado pelo Git.
- A primeira execução controlada da recuperação dirigida, em 11/08/2026, selecionou 50 das 827 NF-es e encontrou o evento `110` com chave CT-e para todas, mas não enviou nenhuma: as 50 consultas ao endpoint ESL de XML retornaram sem documento. A verificação direta confirmou que, para um CT-e desse lote, `GET /api/ctes?key=...` devolve `200 OK` com `data` vazia; para um CT-e já integrado, a mesma credencial e rota devolvem o XML. Logo, não é token, intervalo nem retry: o XML histórico não está disponível na ESL, embora a ocorrência de emissão exista. Os 50 registros ficaram auditados como `ERRO_DESTINO` com a mensagem de XML ausente; não executar os 777 restantes até tratar essa indisponibilidade de origem e a classificação operacional desse caso.
- Em 14/08/2026, o tratamento novo passou a classificar respostas ESL sem XML como `PENDENTE_ORIGEM`, sem nova tentativa automática do mesmo CT-e, sem quarentena e sem repescagem noturna. A validação local direcionada aprovou 11 testes de Vedacit/estado com cliente ESL simulado; nenhum serviço, banco ou endpoint externo foi acionado. O teste legado de criação de porta SOAP continua falhando por divergência entre o WSDL local e as classes SOAP geradas, fora desta alteração.
- Em 12/08/2026, uma investigação SELIA somente leitura consultou as 22 NF-es da planilha `docs/SECRETO/SELIA/Pedidos RODOGARCIA.xlsx` diretamente na ESL, sem POST para a Intelipost nem alteração na auditoria. A ESL retornou eventos intermediários reais: `55` (`Saida para Entrega`), `101` (`Chegada na Filial de Destino`) e `103` (`Mercadoria em Transferência`), além de `1` (entrega), `110` (CT-e emitido) e `114` (POD anexado). A regra anterior filtrava estritamente `occurrence.code == 1`, exigia POD para qualquer envio e, por isso, enviava apenas entrega. A NF 5319 possui evento ESL `1`, mas foi registrada como `IGNORADO`, sem POST, por filtro vigente na execução de 10/08; a NF 5355 possui evento `1`, mas permanece `PENDENTE_FOTO` porque a ESL não fornece POD. Das 22 NFs, 9 constam `ENVIADO`/`SUCESSO`, 11 ainda não têm evento `1` na ESL e uma (5319) requer correção de reprocessamento após a mudança.
- Em 19/08/2026, a validação somente leitura do SFTP SELIA confirmou TCP/22, autenticação e acesso a `/OCOREN/PROCESSED`, em servidor AWS SFTP. A primeira listagem encontrou dois arquivos `OCO*.txt` e dois arquivos iniciais; uma segunda leitura poucos minutos depois não encontrou arquivo `OCO`, evidenciando que a pasta `PROCESSED` é dinâmica e não deve ser tratada como fila estável sem contrato de retenção. Nenhum arquivo foi enviado, alterado ou persistido. A chave SSH foi aceita apenas para a sondagem temporária; produção exige fingerprint oficial e pinning. As credenciais permanecem exclusivamente no `.env` e devem ser rotacionadas por terem sido compartilhadas fora dele.
- As repescagens manuais Vedacit foram separadas por classificação operacional: H e N selecionam somente `PENDENTE_TECNICO`; recusas `BLOQUEADO_DESTINO` não retornam à fila e timeouts ambíguos são tratados exclusivamente pela opção P. H e N iniciam com `VEDACIT_SFTP_RECEIPT_ONLY=true` e reconciliação habilitada, impedindo fallback de comprovante para ESL. A política é coberta por teste direcionado da consulta de seleção.
- O lote J/K materializa novamente a fila a partir do inventário SFTP: registros existentes com XML já em `SUCESSO`, mas canhoto ainda não confirmado, são normalizados para `PENDENTE_FOTO` e voltam ao dreno. `BLOQUEADO_DESTINO` e `TIMEOUT_AMBIGUO` continuam excluídos. O inventário não cria envio sem XML confirmado; esses itens permanecem visíveis para a reconciliação M/O.
- `InventarioSftpVedacitRunner` e `scripts/verificar_inventario_sftp_vedacit.bat` executam comparação somente leitura entre todos os comprovantes válidos do SFTP e o último estado por CT-e na auditoria: confirmados, elegíveis, aguardando XML, bloqueados e sem auditoria. Não chamam SOAP nem alteram a base.
- O `spring-boot-maven-plugin` possui execução explícita de `repackage` no ciclo padrão de empacotamento, para que `mvn package` produza novamente o Fat JAR executável consumido pelos scripts operacionais.
- Em 20/08/2026, o Fat JAR operacional foi recompilado com Java 17: `target/satelite-0.0.1-SNAPSHOT.jar` (79.311.196 bytes, SHA-256 `9C286F15D495A4E66241B9FC99444657AC8C27CC8A2E72BCD1DFC7ECD53E5334`). As execuções reais sem candidatos das opções H e N iniciaram o JAR, consultaram somente a auditoria permitida e terminaram com código `0`; ambas retornaram zero selecionados, sem consulta documental ou envio SOAP.
- O SFTP SELIA será uma integração inteiramente separada da Vedacit: não reutilizar `VedacitSftpClient`, `VedacitSftpPathPolicy`, variáveis `SFTP_RODOGARCIA_*`, inventário de canhotos, auditoria de CT-e/NF-e nem runners Vedacit. O Satélite ainda não contém cliente, contrato de leitura, geração ou upload SELIA/NOTFIS/OCOREN; portanto, não é uma transmissão com falha, e sim uma capacidade independente do AddEvents.
- O contrato inicial do `WORK-SFTP-CLIENTES` está documentado em `database/config/env.example.properties`: `SFTP_CLIENTS_IDS` define a ordem dos identificadores, e cada perfil usa exclusivamente `SFTP_CLIENT_<IDENTIFICADOR>_*` para conexão, fingerprint, base, subpasta e limites. `SftpClientesProperties` devolve somente perfis ativos e rejeita identificador duplicado, credencial/fingerprint ausente, números fora do limite e caminhos fora da base. As variáveis `SFTP_RODOGARCIA_*` permanecem reservadas aos scripts manuais Vedacit até a migração do runner central.
- O `.env` operacional habilita o primeiro perfil `VEDACIT` do `WORK-SFTP-CLIENTES` sem duplicar segredos: cada chave `SFTP_CLIENT_VEDACIT_*` referencia a configuração legada `SFTP_RODOGARCIA_*`; o modo exclusivo de canhoto está habilitado. A validação operacional local foi concluída sem iniciar conexão SFTP ou envio SOAP.
- Em 20/08/2026, o PM2 registrou `WORK-SFTP-CLIENTES` como processo 24 e o Java 17 iniciou normalmente, porém o JAR em execução ainda é anterior ao runner (não contém `WorkSftpClientesRunner`). Portanto não houve inventário, conexão SFTP nem processamento; é obrigatório empacotar o código atual e reiniciar somente esse processo antes de considerar o ciclo de 30 minutos ativo.
- A inicialização headless do `WORK-SFTP-CLIENTES` exige que o construtor de configuração legada de `VedacitSftpClient` seja o construtor Spring explícito; a anotação `@Autowired` impede a ambiguidade com o construtor de perfil multi-cliente.
- `VedacitSftpClientFactory` cria um `VedacitSftpClient` isolado por perfil ativo do `WORK-SFTP-CLIENTES`. Cada operação do cliente mantém conexão SSH/SFTP própria em `try-with-resources`, encerrada antes do próximo perfil; os fluxos legados continuam no cliente identificado como `LEGADO`.
- A migration V14 adiciona `sftp_cliente` nullable à auditoria e o índice `IX_tb_log_integracao_vedacit_cliente_fila`. Ela foi executada pelo operador em 20/08/2026; o histórico permanece intacto e sem cliente até ser tratado pela segregação da fila.
- A migration V15 atribui exclusivamente `VEDACIT` aos registros históricos cuja origem de canhoto já era SFTP ou reconciliação SFTP. Ela foi executada pelo operador em 20/08/2026 e não alterou status, documentos, tentativas ou payloads.
- `SftpDocumentoLockService` usa lock distribuído no SQL Server com `sp_getapplock`, proprietário da sessão e timeout configurável `WORK_SFTP_CLIENTES_LOCK_TIMEOUT_MS` (5 segundos por padrão, limitado a 60 segundos). A aquisição, o envio e a liberação usam a mesma conexão JDBC, eliminando o erro `-999` de lock transacional sem transação SQL ativa. O recurso é determinístico por cliente, NF-e e CT-e. Se o lock estiver ocupado, `WORK-SFTP-CLIENTES` registra o aviso e contabiliza o documento como pendente para a próxima rodada, sem executar SOAP, sem alterar o status de auditoria e sem ocultá-lo como `IGNORADO`; ausência do serviço de lock falha o ciclo de modo explícito. Os testes direcionados cobrem indisponibilidade do lock e preservação da pendência; o mock de `JdbcTemplate.execute` usa testemunha de tipo explícita, sem conversão unchecked no compilador/IDE.
- A materialização SFTP por cliente agora consulta também o legado Vedacit arquivado pelo par exato NF-e/CT-e quando não há registro ativo do cliente: sucesso histórico completo cria apenas auditoria ativa já concluída, sem SOAP; XML/dados históricos confirmados com comprovante pendente criam candidato `PENDENTE_ENVIO` para o comprovante SFTP; ausência de dados históricos continua `BLOQUEADO_ORIGEM`. A seleção não reenvia XML. Os testes direcionados cobrem os cenários de sucesso histórico completo e de XML histórico sem comprovante.
- Em 21/08/2026, o JAR Java 17 com a correção do lock por sessão foi publicado (SHA-256 `5FD62891EFF8C6933682B68F217B04861DBF5B452E4DBB81A604B515318A4057`), a API passiva retornou HTTP 200 e o `WORK-SFTP-CLIENTES` iniciou uma execução real: os locks foram obtidos e três canhotos foram confirmados pela Vedacit. Dois candidatos sem arquivo correspondente permaneceram como `PENDENTE_FOTO`, sem consulta ESL; o worker segue processando sequencialmente os itens restantes.
- `WORK-SFTP-CLIENTES` é o worker foreground/headless de ciclo único para a Vedacit: percorre perfis SFTP habilitados sequencialmente, materializa, seleciona e executa a quarentena técnica por `sftp_cliente`, usa `sp_getapplock` por cliente/NF-e/CT-e durante o envio SOAP e mantém cada fonte documental no perfil corrente, sem consulta ESL. Falhas de conexão ou processamento de um perfil não interrompem os seguintes. O PM2 supervisiona somente uma instância, com logs próprios e nova execução 30 minutos após o fim; os scripts `validar_`, `iniciar_`, `logs_` e `parar_work_sftp_clientes.bat` são a interface operacional.
- A migration V16 cria `tb_work_sftp_cliente_execucao`, auditoria técnica sem documento, caminho, payload ou credencial, para cada ciclo do worker. O Dashboard consulta somente a projeção SQL do último ciclo por cliente em `GET /api/auditoria/vedacit-sftp/clientes`; a próxima execução é estimada a partir do término mais 30 minutos, sem contato com ESL, SFTP ou SOAP.
- A auditoria de ciclos SFTP também expõe `GET /api/auditoria/vedacit-sftp/execucoes`: histórico somente leitura, paginado no SQL Server e ordenado pelo término decrescente, filtrável por cliente, status e intervalo. O contrato mantém apenas horários, conexão, resultado e contagens técnicas auditadas, sem documento fiscal, caminho SFTP, payload ou credencial.
- O histórico SFTP possui cobertura de repositório para filtros sargáveis, ordenação e ausência de colunas sensíveis, além de cobertura do controller; ambos executaram com Java 17 sem banco, ESL, SFTP ou SOAP.
- A API passiva é iniciada somente pelo PM2 via `scripts\iniciar_api_dashboard.bat` (opção 6 do `satelite.bat`). O script verifica a porta 19090 e informa PID/processo que a ocupa sem encerrar nada; só inicia `Satelite-API-19090` se ela estiver livre.
- Os testes direcionados do Dashboard confirmam que o `CommandLineRunner` é substituído por no-op, a rota SFTP retorna somente o DTO agregado e a quarentena mutável retorna `403` no modo passivo. O teste do runner confirma que a falha de um perfil é auditada e não impede o perfil seguinte; ele não cria cliente ESL nem dispara qualquer integração externa.
- Em validação concorrente real no SQL Server `SATELITE_TMS_AUDITORIA`, a primeira transação obteve `sp_getapplock` exclusivo (retorno `0`) e a segunda, para o mesmo cliente/NF-e/CT-e, recebeu `-1` com timeout zero. Ambas encerraram sem DML e sem reter o lock após o rollback da transação proprietária.
- A projeção de ciclos foi validada em transação revertida na mesma base com os clientes sintéticos `DASH_TEST_A` e `DASH_TEST_B`: retornou exatamente uma linha isolada por cliente e nenhum dado permaneceu após o rollback.
- `SateliteApplicationTests` inicia o contexto web no modo `APP_DASHBOARD_API_ONLY=true` com scheduler e worker SFTP desligados; os runners existentes são bloqueados e os mocks de clientes externos permanecem sem interação durante a inicialização.
- A prévia somente leitura de 20/08/2026 para a fila SFTP `VEDACIT` confirmou 152 itens com `status_dados=PENDENTE_ORIGEM`/`status_canhoto=PENDENTE_FOTO`, que permanecem fora de qualquer reenvio até a origem fornecer XML. Há 14 classificações residuais com ambos os estágios em `SUCESSO` (12 `PENDENTE_ENVIO`, 1 `PENDENTE_TECNICO` e 1 `BLOQUEADO_DESTINO`) e 30 `ERRO_DESTINO` técnicos com mensagem não categorizada; nenhum desses grupos foi alterado pela prévia.
- A V17 foi validada em transação revertida e aplicada em 20/08/2026 exclusivamente em `SATELITE_TMS_AUDITORIA`: reclassificou os 14 sucessos residuais SFTP `VEDACIT` para `SUCESSO`. A conferência posterior retornou 1.355 sucessos SFTP sem classificação divergente; não houve SOAP, ESL, exclusão ou alteração de arquivo.
- A V18 identificou por assinatura sanitizada que os 30 `ERRO_DESTINO` técnicos SFTP decorriam de nomes sem NF-e/CT-e válidos. Após prévia transacional de 30 linhas, a migration idempotente os reclassificou para `BLOQUEADO_ORIGEM`; ficam permanentemente fora de retry, sem SOAP, ESL ou alteração de arquivo.
- A V19 foi aplicada em `SATELITE_TMS_AUDITORIA` para proteger pendências SFTP sem confirmação ativa de XML/CT-e; a classificação não prova, por si só, ausência histórica. A revisão somente leitura de 21/08/2026 encontrou 521 bloqueios ativos com NF-e, CT-e e caminho SFTP válidos: 108 possuem XML e comprovante já confirmados em log legado arquivado, 363 possuem XML confirmado no legado mas ainda sem comprovante confirmado, e somente 50 não possuem sucesso histórico de XML. O efeito foi criado porque a materialização por `sftp_cliente=VEDACIT` não reconcilia o par NF-e/CT-e exato já arquivado pela V21. Nenhum desses 521 foi reenviado, reclassificado ou excluído durante a revisão.
- A V20 foi aplicada em `SATELITE_TMS_AUDITORIA`: os 30 nomes SFTP sem NF-e/CT-e foram confirmados como `BLOQUEADO_ORIGEM`. Upload instável continua `PENDENTE_TECNICO`, mas nome documentalmente inválido passa a bloqueio desde a materialização; não houve ESL, SOAP nem alteração de arquivo nessa classificação.
- A revisão de 20/08/2026 confirmou que a reconciliação histórica V10 já está aplicada e preserva 1.254 vínculos (`1.250` exatos e `4` complementares). Os fluxos manuais M/O e a recuperação dirigida dependem de XML ESL e não integram o `WORK-SFTP-CLIENTES`; portanto não são necessários à operação SFTP atual. O legado sem `sftp_cliente` permanece preservado e separado: 40 bloqueios de destino, 30 bloqueios de origem, 134 pendências de envio, 866 sem classificação e 1 timeout ambíguo; nenhum será reenviado automaticamente.
- O artefato padrão foi recompilado com Java 17 após liberar os processos que o mantinham aberto: `target/satelite-0.0.1-SNAPSHOT.jar`, SHA-256 `5C82344040E710FC56E9079A8E19F3A57C750FF1D709F26DC49E365DE8668440`. O `logback.version` foi fixado em `1.5.16`, pois a dependência transitiva `1.5.34` não continha `ThrowableProxy` exigida na inicialização; a API passiva foi reiniciada e validada em `GET /api/auditoria/vedacit-sftp/clientes` com HTTP 200, sem habilitar integrações externas. O `WORK-SFTP-CLIENTES` foi iniciado após V19 e concluiu ciclo saudável em 20/08/2026: conexão `OK`, 1.499 arquivos válidos, 30 rejeitados auditados, 3 selecionados, nenhum envio/erro e 152 bloqueios de origem; o PM2 aguarda 30 minutos para o próximo ciclo.
- As opções manuais M/O foram removidas do `satelite.bat`; os scripts de prévia reconciliada, lote reconciliado e recuperação dirigida agora encerram com bloqueio explícito e não iniciam Java, ESL ou SOAP. O JAR Java 17 atualizado (SHA-256 `18409A8DB49D9431CF0397BD8B1617E52D1FCE1AEA893900EB1A7D14FFF2C8F3`) foi publicado e validado: a API passiva retornou HTTP 200 e o novo ciclo `WORK-SFTP-CLIENTES` concluiu com conexão `OK`, 1.499 válidos, 30 rejeitados, 3 selecionados, zero envio/erro e 182 bloqueios de origem; o PM2 aguarda o próximo ciclo de 30 minutos.
- A V21 foi aplicada em `SATELITE_TMS_AUDITORIA`: os 4.552 registros Vedacit sem `sftp_cliente` receberam `arquivado=1`, data e motivo `LEGADO_SEM_CLIENTE_SFTP`, sem exclusão física, ESL, SFTP ou SOAP. As listagens, métricas consolidadas, evolução, pendências, quarentena e resumo do Dashboard filtram registros arquivados; a consulta JPA de quarentena passou a aplicar o mesmo critério tanto na página quanto na contagem. O JAR Java 17 (SHA-256 `A2F8897A19AF2D47809AA532C7DCA2205EA47FEF075FEE5626A5C81230E6C2CF`) foi publicado; a API passiva retornou HTTP 200 e confirmou 1.541 registros Vedacit ativos tanto na paginação quanto na métrica consolidada. O ciclo reiniciado do worker concluiu saudável: conexão `OK`, 1.499 válidos, 30 rejeitados auditados, 3 selecionados, zero envio/erro, 182 bloqueios e próximo ciclo pelo PM2 em 30 minutos. A reconciliação posterior identificou que o filtro atual por cliente não reaproveita o par NF-e/CT-e exato do legado arquivado; essa lacuna deve ser corrigida antes de qualquer reenvio.
- A auditoria operacional do Dashboard exclui do escopo de pendências, métricas, evolução e resumo os bloqueios SFTP Vedacit (`BLOQUEADO_ORIGEM`/`BLOQUEADO_DESTINO`), que não são reenviáveis até sua reconciliação técnica e já possuem contagem própria na telemetria do worker. Em 21/08/2026, 521 bloqueios de origem exigem reconciliação: 108 são sucessos completos históricos, 363 têm somente os dados/XML históricos confirmados e 50 continuam sem confirmação histórica de XML. Os 32 arquivos sem chaves válidas permanecem bloqueio definitivo de origem; os 5 PPG e 2 SELIA remanescentes são históricos e aguardam classificação explícita, sem alteração automática.
- A seleção de NF-es do inventário multi-cliente ignora explicitamente documentos e chaves nulos em um loop tipado antes de consultar a fila, mantendo a segurança de nulidade estática e evitando que um inventário anômalo interrompa o ciclo.
- Em 12/08/2026 foi implementada a base segura da linha do tempo SELIA: `SELIA_INTELIPOST_ALL_EVENTS_ENABLED=false` mantém a consulta ESL filtrada em `occurrence_code=1`; `SELIA_INTELIPOST_EVENT_CODE_MAP` aceita o DePara no formato `codigo_esl=codigo_cliente`. O DePara foi refeito e confirmado como oficial em 13/08: `1 -> 1`; `2 -> 72`; `6 -> 10`; `14 -> 45`; `25 -> 3`; `27 -> 55`; `46 -> 11`; `47 -> 57`; `52 -> 18`; `55 -> 85` (`TO_BE_DELIVERED` / `SAIU PARA ENTREGA`); `58 -> 59`; `59 -> 9`; `76 -> 60`; `79 -> 53`; `85 -> 61`; `91 -> 15`; `101 -> 7`; `103 -> 58`; `110 -> 80`; `135 -> 62`. O mapa anterior foi integralmente substituído; códigos não listados não serão enviados. A coleta de todos os eventos continua desligada. Quando ativada e mapeada, a consulta passa a buscar todos os eventos, aceita somente códigos configurados e mantém idempotência por `occurrence_id`; eventos intermediários são enviados sem POD e entrega (`1`) continua exigindo POD. A página recebida da ESL é ordenada por data da ocorrência antes do envio. Um registro SELIA `IGNORADO` sem `request_payload` nem `response_payload` deixa de bloquear somente sua reexecução controlada, sem alterar a idempotência de eventos enviados. O worker foi pausado durante a correção de configuração; não houve registro de auditoria ou log de AddEvents no intervalo. A validação local de SELIA/estado/orquestração executou 48 testes, todos aprovados.

## Protocolo de Planejamento de Requisições

- Antes de iniciar qualquer planejamento ou escrita de código, a IA DEVE OBRIGATORIAMENTE ler `AGENTS.md` do projeto local e `CONTEXTO_GLOBAL.md` quando presente no workspace.
- O `CONTEXTO_GLOBAL.md` dita as regras do ecossistema e o `AGENTS.md` dita as regras locais. Falhar em ler e aplicar essas regras resulta em quebra arquitetural.
- Ao receber uma nova requisição para este projeto, atuar como Arquiteto de Software e usar este `states.md` como ESTADO ATUAL.
- A análise deve respeitar a stack, a arquitetura, as fronteiras de banco e os contratos de dados descritos neste arquivo.
- A resposta de planejamento deve retornar somente o bloco `## Tarefas Pendentes`, formatado em Markdown.
- O bloco deve decompor a requisição em tarefas sequenciais, lógicas e granulares, especificando arquivos exatos, variáveis, tipagens e validações que deverão ser alterados ou criados.
- É proibido incluir saudações, conclusões, explicações fora dos bullets ou reescrever outras seções durante a resposta de planejamento.

## Tarefas Pendentes

- [ ] [P0 / Relações Vedacit] Tratar conjuntamente as planilhas auditadas em 10/09: 618 XMLs disponíveis sem sucesso local; 695 comprovantes SFTP bloqueados por dados, dos quais 629 com XML correlacionado; 101 comprovantes indicados na ESL pelo usuário, mas ausentes no SFTP; 74 divergências de sucesso de XML e duas de canhoto. Preservar a sobreposição de 321 NF-es entre as listas, conciliar o CT-e exato e manter idempotência/locks/status de bloqueio. Definir recuperação dirigida dos documentos ausentes no SFTP sem habilitar fallback global nem reenviar sucessos por suposição. Planilhas anotadas e relatório já gerados; nenhum envio executado.
- [ ] [P0 / Alternância XML e comprovantes] A auditoria das 23:26 confirmou o gargalo: o runner só sincroniza/processa comprovantes depois de esgotar a paginação XML; `maxPaginas` pausa e continua, sem devolver controle. Intercalar passagens finitas de XML e comprovantes na mesma execução, mantendo retomada de cursor, XML confirmado antes do comprovante do mesmo par, pausas, locks, isolamento por cliente e retenção de recusas/timeouts. Não introduzir teto diário nem reenvio de sucessos. Cobrir o cenário de origem com mais de dez páginas e comprovantes liberáveis antes de seu término. Às 23:26, 252 pares SFTP já tinham XML novo confirmado mas aguardavam a promoção de `BLOQUEADO_ORIGEM`; validar arquivo e aceite real ao entrar nessa etapa. Diagnóstico completo em `docs/verificacao-xml-apos-reinicio-2026-09-10.md`.
- [ ] [P0 / Download XML ESL recusado] No ciclo de 10/09 iniciado às 23:13, seis downloads XML pela ESL retornaram HTTP 401 na página 18. O caminho alternativo de `baixarXmlCteEsl` usa `RODOGARCIA_MASTER_API_REST`; verificar a validade e o escopo da credencial específica sem substituir por outro token por suposição ou expor seu conteúdo. Obtenção SFTP e 304 aceites XML já foram confirmados. A página registrou oito resultados de erro e reteve o cursor 305833355, portanto ainda há origem por percorrer; manter idempotência dos já aceitos na retomada. Não interpretar 401 como limite diário nem como recusa de canhoto pela Vedacit.
- [ ] [P0 / Fechamento do ciclo XML e comprovantes] A ativação e os primeiros envios das duas etapas foram confirmados: 304 CT-es com XML aceito e três novos comprovantes às 23:29:55, com total do período 270 e 277 pendentes. A varredura XML retornou após erro na página 18; o dreno SFTP continuava. Acompanhar o fim da passagem, respostas lentas do destino, saldo e retomada XML; não considerar a fila esgotada pela amostra inicial. Recuperação dirigida preparada com 695 pares das planilhas permanece separada do incremental; não resetar cursores/sucessos divergentes. Evidências em `target/verificacao-comprovantes-20260910-2319/` e procedimento em `docs/correcao-fluxo-vedacit-2026-09-10.md`.
- [ ] [P1 / Contagem do resumo XML] Separar a contagem de sucesso da etapa XML no resultado de página/runner: `converterResultadoRegistro` retorna `PENDENTE_FOTO` quando o XML foi aceito, mas o canhoto continua pendente. Na primeira página de 10/09 às 23:16, o resumo mostrou duas enviadas apesar de 17 aceites XML distintos; o endpoint por etapa contou os aceites corretamente. Corrigir somente a telemetria/resultado do fluxo XML, preservando o status pendente do canhoto, e cobrir o caso em teste antes de uma nova publicação.
- [ ] [P1 / Reconciliação residual] Conferir na ativação a baixa dos três bloqueios que têm sucesso ativo no par NF-e/CT-e exato; a conciliação automática dessas confirmações já foi corrigida e testada. O comprovante com CT-e divergente permanece pendente, sem trocar CT-e apenas pela NF-e. Os casos sem XML/arquivo continuam dependentes da origem.
- [ ] [Publicação / Integrações] Disponibilizar o candidato Satélite `FA925E2E...`, a API Dashboard `BE8C2080...` e o frontend revisado em 10/09. A compatibilidade frontend já identifica SFTP nas respostas antigas; os candidatos Java acrescentam o campo e o filtro no servidor. Pacotes e processos operacionais não foram substituídos. Conferir origem, paginação, rótulos e tooltips após a atualização humana. O histórico continua restrito aos ciclos SFTP: monitorar os fallbacks ESL dos outros processos exige auditoria de sua execução e da fonte efetivamente utilizada, não implementada nesta revisão visual/contratual.
### URGENTE — WORK-SFTP-CLIENTES / estouro de parâmetros no SQL Server

- Diagnóstico somente leitura confirmado em 07/09/2026: o ciclo mais recente autenticou, validou a pasta e listou o SFTP, mas falhou na primeira seleção JDBC. A SQL real recebeu 2.796 parâmetros — 2.794 NF-es, `TOP (?)` e `sftp_cliente=?` — e ultrapassou o teto operacional do SQL Server. A exceção imprime a mesma SQL duas vezes; portanto, 5.592 não é a quantidade real. O primeiro estouro ocorreu em 26/08/2026 às 12:22:15 com 2.098 NF-es mais dois parâmetros; o ciclo anterior, às 11:51:24, foi o último concluído. Desde então foram 537 ciclos sem sucesso: 532 estouros SQL e 5 timeouts TCP SFTP. Não há evidência de falha de senha, fingerprint, autenticação ou permissão. O painel grava qualquer exceção como `conexao=FALHA` e zera as métricas, logo `0 válidos` não representa inventário vazio.
- Baseline operacional somente leitura de 07/09/2026: há 2.867 logs ativos do cliente SFTP `VEDACIT`, 2.794 NF-es distintas auditadas e 232 registros `PENDENTE_ENVIO`, correspondentes a 230 NF-es candidatas. Desses 232 registros, 231 ainda têm zero tentativa de canhoto. Não reiniciar o worker após a correção sem limitar e autorizar explicitamente o primeiro lote, pois o retorno do ciclo volta a permitir chamadas SOAP.
- Em 07/09/2026 às 11:45, houve uma reinicialização humana do `WORK-SFTP-CLIENTES`, porém o PM2 continuou apontando para `target/satelite-0.0.1-SNAPSHOT.jar` alterado em 21/08/2026 às 15:11. O ciclo terminou às 11:46 com a mesma SQL antiga e o erro de mais de 2.100 parâmetros; não considerar essa tentativa como validação da correção. Às 12:05 a build nova foi concluída de forma isolada e o JAR padrão foi restaurado com o artefato executável atualizado, SHA-256 `A77DB9AD8428E5B47553955D0534EB3F83B7511B5E9404A2EB17BE8EEB937751`. O PM2 ainda não iniciou um novo processo com esse hash. A configuração produtiva `SFTP_RODOGARCIA_MAX_FILES_PER_CYCLE` foi reduzida de 100 para 10 antes da próxima inicialização, que deve permanecer supervisionada.
- Validação humana iniciada às 12:15 de 07/09/2026 com PID 43580 e o JAR novo: a execução ultrapassou a seleção de fila sem erro de 2.100 parâmetros, confirmando o particionamento em produção. A fila passou a processar no limite de 10 itens. Os dois primeiros retornaram `Canhoto indisponível no SFTP` sem consulta ESL; o item seguinte ficou aguardando a resposta HTTPS SOAP da Vedacit dentro do prazo total de 210 s. Trata-se de comportamento externo separado da falha SQL corrigida; não elevar o limite nem considerar o ciclo concluído antes do resumo final e da classificação persistida.
- O perfil Maven `fat-jar-alternativo` agora aponta para a execução real `default` do `spring-boot:repackage` e anexa o executável com classificador `vedacit-fix`. O `pom.xml` também permite escolher `satelite.build.directory`, usado para empacotar em diretório isolado quando `target` estiver em uso pelo IDE ou por um artefato anterior.
- [x] [P0 / Particionamento] `EtlRepescagemService` agora possui uma política única de blocos de até 500 NF-es, aplicada às filas normal e técnica, ao saldo do worker e às contagens/caminhos legados. A consulta antiga com `NOT IN` das NF-es já tentadas foi removida do repositório, eliminando a segunda lista de parâmetros crescente.
- [x] [P0 / Ordenação e idempotência] Todos os resultados dos blocos são unidos, ordenados por `dataProcessamento ASC, id ASC`, desduplicados por NF-e e limitados somente no final. As NF-es já tentadas são retiradas em memória antes da consulta; a técnica só é consultada após a normal ficar vazia em todos os blocos.
- [x] [P0 / Não truncar inventário] O inventário continua completo; a correção é exclusivamente na consulta ao banco, sem `limit` sobre a listagem SFTP.
- [x] [P0 / Consultas e saldo] As consultas SFTP de fila usam `COALESCE(l.arquivado, false) = false` e classificação explícita (`PENDENTE_ENVIO` ou `PENDENTE_TECNICO`). Foi criada a contagem distinta por cliente e o saldo deixou de depender de `.size()` de uma página, passando a somar `COUNT(DISTINCT chaveNfe)` por bloco.
- [x] [P0 / Limites por cliente] `maxArquivosPorCiclo` passou de `SftpClientesProperties.Perfil` pela fábrica até o runner. `WORK_SFTP_CLIENTES_MAX_ITEMS` permanece somente como teto global defensivo; o menor valor entre ele e o perfil é usado no ciclo.
- [x] [P0 / Cobertura] Testes locais sem rede/banco externo cobrem 2.794 NF-es em seis blocos de até 500, candidato no último bloco, ordem global, deduplicação, contagens acima de 500, filtros de fila e limites por perfil. Em 07/09/2026 passaram: `EtlRepescagemServiceTest` (19), `WorkSftpClientesRunnerTest` (2), `SftpClientesPropertiesTest` (4), `VedacitSftpClientFactoryTest` (1) e `LogIntegracaoRepositoryTest` (3). A ordenação e os captors de teste não usam conversões não verificadas, mantendo o analisador Java sem avisos de tipos anuláveis/genéricos.
- [ ] [P1 / Escalabilidade] Eliminar o padrão N+1 em `EtlRepescagemService.sincronizarInventarioSftpVedacit`: atualmente cada documento válido dispara ao menos uma consulta individual e o ciclo já percorre 2.794 NF-es. Buscar previamente, em blocos seguros, os registros ativos do cliente e os legados exatos por NF-e/CT-e, indexá-los por chave em memória de forma limitada e gravar somente diferenças. Validar que documentos já auditados não recebem nova escrita nem alteração de data apenas por aparecerem novamente no inventário.
- [ ] [P1 / Motivo visível no painel] Completar o detalhamento de falhas em `WorkSftpClientesRunner`: a revisão de 09/09 já preserva `conexao=OK` após autenticação e sinaliza erros de processamento/auditoria no código de saída; restam reter métricas parciais quando uma exceção interrompe o ciclo e classificar a etapa como `CONEXAO`, `INVENTARIO`, `BANCO_FILA` ou `PROCESSAMENTO`. Persistir código e motivo sanitizado na auditoria por migration idempotente `database/sql/migration/V22__detalhar_falha_ciclo_work_sftp.sql` e espelhar o schema base. Atualizar `WorkSftpClientesAuditoriaRepository`, `WorkSftpClienteStatusDTO`, respectivos testes e o endpoint existente, sem guardar SQL completa, caminho, documento, payload ou credencial.
- [ ] [P1 / Dashboard] No `etl-dash`, propagar `etapaFalha`, `codigoErro` e `motivoErro` pelo proxy já transparente e tipá-los em `dashboards/frontend/src/api/endpoints/integracoesServico.ts`; exibir a razão sanitizada nos cartões e no histórico de `dashboards/frontend/src/pages/IntegracoesPage.tsx`. Atualizar `integracoesServico.test.ts`, `kpiDictionary.ts` quando o texto operacional mudar e os testes do backend `IntegracoesServiceTest`. O painel deve distinguir “SFTP indisponível” de “Falha no banco” e não apresentar inventário zero como fato quando a falha ocorreu depois da listagem.
- [ ] [P1 / Logs] Substituir `WorkSftpClientesRunner.resumir(Exception)` por classificação sanitizada da causa raiz, limitando tamanho e impedindo que Hibernate repita a SQL completa no log. Preservar classe/código técnico suficiente para diagnóstico e testar que segredo, chave fiscal, SQL expandida e payload não aparecem.
- [ ] [Entrega controlada] SQL dirigido, contratos e JAR Java 17 foram validados em 09/09/2026; suíte com 300 aprovados e três manuais ignorados, sem falhas. O pacote recompilado e equivalente ao candidato já está no caminho padrão do PM2 desde 23:31 BRT, SHA-256 `EBC700C71B264A50EC933FDD290B3761DBB915152420AE5D6626746ADB7F60E2`, com backup anterior preservado. O operador retomará a API passiva e o worker. A prévia encontrou apenas um candidato sem comprovante disponível: falta massa elegível para confirmar envio real. Quando disponível, acompanhar um ciclo supervisionado com limite explícito de dez NF-es e condução humana do runtime, conferindo conexão, inventário, ordem, locks, auditoria, respostas SOAP e saldo. Não iniciar/reiniciar PM2 automaticamente nem usar sucessos/bloqueios/timeouts ambíguos como massa. Somente após esse aceite avaliar 50 e 100 por ciclo. Artefato e evidências em `docs/validacao-pacote-2026-09-09.md`.

### Fluxo mestre — investigação, decisão e aplicação segura

- [ ] Vedacit reconciliação SFTP: o código e 21 testes direcionados já validam a correlação exata do legado arquivado; empacotar com Java 17, validar o JAR no PM2 e executar o envio controlado somente dos 363 candidatos com XML/dados históricos confirmados. Preservar os 108 sucessos completos sem SOAP e os 50 sem XML histórico em `BLOQUEADO_ORIGEM` até prévia somente leitura da origem.
- [ ] Vedacit legado: separar sucesso, bloqueio definitivo, timeout ambíguo e pendência recuperável; propor somente arquivamento lógico para registros que não pertencem ao SFTP, preservando auditoria e sem `DELETE`/`TRUNCATE`.
- [ ] Auditoria residual PPG/SELIA: classificar os 5 PPG e 2 SELIA ainda exibidos como pendência operacional; após relatório somente leitura aprovado, propor apenas arquivamento lógico ou manutenção como pendência ativa. Não reprocessar, consultar ESL ou alterar os registros automaticamente.
- [ ] Dashboard e operação: acrescentar no painel os agregados de ciclo, origem, classificação e fila por cliente, além de telemetria ESL; confirmar em cada implantação que a API passiva não habilita integrações externas.
- [ ] SELIA: executar investigação/homologação em ciclo isolado, primeiro por contrato e massa de teste, depois por validação de eventos, POD, cursor e idempotência; não habilitar fluxo produtivo sem aceite.
- [ ] SUPPORTE/Intelipost: tratar como dependência externa de contrato e massa; implementar ou habilitar somente após resposta verificável da origem/destino.
- [ ] Fechamento operacional: para cada bloco, executar testes direcionados, empacotar com Java 17, validar o JAR em uso pelo PM2 e registrar no `states.md` o resultado, a decisão e a próxima ação humana quando houver.

### SELIA — rastreamento completo (prioridade atual)

- [ ] Em ciclo isolado, definir `SELIA_INTELIPOST_ALL_EVENTS_ENABLED=true` e validar consulta ESL sem `occurrence_code`, ordenação cronológica, uma publicação por `occurrence.id` e cursor produtivo inalterado.
- [ ] Validar na homologação que `55`/`91`/`101`/`103`/`110` sejam enviados sem POD, que o evento `1 -> 35` continue `PENDENTE_FOTO` sem comprovante e que `114` não seja enviado como tracking. A base está implementada e o rastreamento completo permanece desligado até esse aceite.
- [ ] Reprocessar controladamente a NF 5319: o bloqueio técnico do log SELIA `7785` foi corrigido para o caso `IGNORADO` sem request/response. Confirmar a ocorrência `1`, POD e correlação pedido-volume; executar um único POST autorizado e verificar a auditoria sem duplicidade.
- [ ] Manter a NF 5355 em pendência de POD e solicitar à ESL/Rodogarcia a disponibilização do comprovante; o evento `1` existe, porém a consulta oficial de comprovante retorna vazia. Não enviar entrega sem anexo.
- [ ] Acompanhar as 11 NFs sem `occurrence.code=1` na ESL. Após a implantação, poderão receber eventos intermediários disponíveis; a entrega só será enviada quando a origem disponibilizar o evento `1` e o POD.
- [ ] [SELIA / SFTP separado] Obter da Intelipost a fingerprint SSH oficial, contrato do arquivo OCOREN/NOTFIS (layout, codificação, tipos de registro, semântica de `PROCESSED`, diretório de entrada e retenção), gatilho, confirmação de processamento e responsabilidade de upload/download. Não inferir layout a partir de arquivos transitórios em `/OCOREN/PROCESSED`.
- [ ] [SELIA / SFTP separado] Após o contrato, criar pacote próprio `services/origem/sftp/selia` e propriedades exclusivas `SELIA_SFTP_*`, com host-key pinning, allowlist de caminho, estabilidade de arquivo, leitura/escrita explicitamente autorizada, auditoria SELIA própria e testes isolados. Não reutilizar componentes ou configurações Vedacit.

### Vedacit — pendências de origem e comprovantes

### SUPPORTE e Intelipost — pendências externas

- [ ] Solicitar à ESL/SUPPORTE uma ocorrência de entrega realizada (`code == 1`) para um dos seis pares NF-e/CT-e recebidos no NOTFIS, ou massa equivalente. As seis consultas retornaram `data` vazia; assim que houver ocorrência, aplicar whitelist e habilitação humana controlada de `APP_SUPPORTE_ENABLED=true`.
- [ ] Opcionalmente, solicitar à Intelipost o escopo de leitura para `GET /shipment_order/invoice_key/{chave_nfe}` e `GET /shipment_order/get_volumes/{pedido_envio}`. Não incorporar essas consultas enquanto retornarem HTTP 401; elas não bloqueiam a PLP.

### Consumo ESL, segurança e operação supervisionada — pendências de código

- A validação manual de conectividade GraphQL do ETL foi centralizada no `GerenciadorRequisicaoHttp`, portanto agora respeita o throttle global, retry e circuit breaker como as demais chamadas. Os testes direcionados `GraphQLConnectivityValidatorTest` e `GraphQLPaginatorTest` passaram; a suíte completa do ETL excedeu o tempo de execução local e não é evidência de conclusão das pendências maiores.
- O rate limit da PLP foi validado pelos testes direcionados `SeliaPlpRateLimitServiceTest` e `SeliaPlpRateLimitInterceptorTest` (4 testes aprovados): a mesma chave autenticada é limitada mesmo com mudança de IP, IP sem chave válida possui limite próprio, a janela expira corretamente e o `429` não expõe o segredo. A cobertura MVC adicional confirmou que chave inválida ainda recebe `401` após passar pela proteção e que o replay autenticado da mesma PLP preserva a idempotência, sem nova gravação de auditoria (2 testes locais aprovados).
- O filtro `SeliaPlpRequestSizeFilter` limita o corpo da PLP antes da desserialização, inclusive quando a requisição não declara `Content-Length`; o máximo configurável `SELIA_INTELIPOST_PLP_MAX_BODY_BYTES` é 5 MiB por padrão. O corpo válido é preservado para o controller e excedentes recebem `413` sanitizado sem encaminhamento. Os três testes locais de tamanho declarado, chunked e corpo válido passaram sem rede, banco ou serviço iniciado.
- A telemetria ESL foi compilada e exercitada isoladamente com sucesso para `VEDACIT`/`CTE_XML`: uma chamada de sucesso gera evento `200` e uma simulação local de `429` gera exatamente três eventos (tentativas 1, 2 e 3; retry apenas nas duas últimas) antes de encerrar. O mesmo teste local confirmou o bloqueio da terceira chamada pela mesma chave da PLP. A regressão de compatibilidade do método legado foi corrigida preservando o nome operacional nas exceções. O bloqueio anterior de compilação por `.class` Vedacit em uso foi contornado em 09/09/2026 com saída isolada e goals Maven offline: a suíte unitária concluiu com 181 aprovados e 3 manuais ignorados, sem acessar ESL, banco ou serviços operacionais. A geração SOAP pelo ciclo Maven padrão continua uma validação distinta.
- [ ] [Cobertura / Lacunas] A suíte de 303 testes do candidato cobre 67,88% das linhas e 53,54% dos desvios do código próprio. Aprofundar cenários de `EtlRegistroService`, `EtlRepescagemService`, integrações e runners legados conforme o inventário por classe; não remover classes nem declarar validação integral de produção com base no percentual. Revalidar os eventos ESL quando o serviço/limite estiver normalizado e confirmar os 37 nomes de comprovantes fora do padrão com a origem, sem ampliar aceitação por suposição.

- [ ] [Externo / Contrato] Validar posteriormente por canal confiável a fingerprint SSH fixada por confiança inicial e obter estrutura/subpasta de cada cliente, padrão de nome/correlação CT-e/NF-e, formatos, estabilidade de upload, retenção e limites. A conta compartilhada e a fingerprint permanecem somente no `.env`; não reproduzir seus dados fora dele.
- [ ] [Definição operacional] A primeira configuração está definida em `.env` como `SFTP_RODOGARCIA_CLIENT_PATH=/g_rodogarcia/VEDACIT`. Para os demais clientes, preencher exatamente uma subpasta abaixo de `/g_rodogarcia`; bloquear o processo quando estiver vazia, apontar para a raiz ou tentar sair da base. Não inferir subpasta por nome de arquivo nem listar outros clientes.
- [ ] [Média / Código] Evoluir o adaptador `services/origem/sftp/vedacit` com cursor técnico/histórico e telemetria de fonte. Cliente read-only, host key pinning, allowlist, verificação de upload estável, preferência SFTP de XML/POD e fallback ESL já estão implementados; a ESL continua exclusivamente como fonte de ocorrências e fallback documental.
- [ ] [Crítico / Código] A `EslRequestPolicyService` registra cada tentativa ESL em `tb_esl_request_telemetria` com origem, destino, rota/template fechado, status HTTP, tentativa, retry, fallback, cache e duração, sem token, payload ou documento fiscal. A V9 já está aplicada; faltam agregados horário/diário/mensal e alerta de anomalia.
- [ ] [Alta / Código] Tornar o PM2 o único supervisor contínuo do Satélite: ajustar `ecosystem.config.js`, `satelite.bat` e os auxiliares de execução para exigir modo foreground explícito e impedir segundo processo por lock compartilhado/porta. Incluir verificação de processo/JAR/porta e falha segura quando uma instância manual conflitar com `Satelite-API-19090`, `ETL-SELIA-Worker` ou `ETL-Vedacit-Worker`.
- [ ] [Alta / Código] Expor os agregados somente de leitura de consumo e anomalias em `src/main/java/com/example/satelite/controllers/` para o Dashboard: rota/template, origem, destino, status HTTP, tentativa, retry, fallback, cache hit/miss e intervalo horário/diário/mensal. O Dashboard não pode consultar a ESL diretamente nem exibir segredo ou dado fiscal completo.
- [ ] [Alta / Código] Em `EtlFluxoDestinoService`, desacoplar leitura e distribuição: buscar cada página de ocorrências ESL uma vez por escopo compatível e distribuir os itens aos destinos elegíveis, preservando cursor e idempotência por destino. Medir antes/depois para comprovar que não houve perda ou duplicidade de integração.
- [ ] [Alta / Código] Em `EtlRegistroService`, `VedacitIntegrationService` e `EtlRepescagemService`, persistir cache negativo de XML e comprovante ausentes, com razão, data da última consulta e cooldown configurável por recurso. Nova consulta só é permitida após expiração ou nova evidência de alteração na ocorrência.
- [ ] [Média / Código] Adicionar validação do artefato/JAR antes de abrir a porta pública, com healthcheck degradado e log técnico para incompatibilidades. `SateliteApplicationTests` já cobre o carregamento do contexto e evitou a regressão de injeção do rate limit, mas ainda não valida o JAR final em uso pelo PM2.
